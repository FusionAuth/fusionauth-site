---
const { name, since } = Astro.props;
---
<span class="bg-blue-100 dark:bg-blue-900 border-b-2 border-b-indigo-500 break-words">{name && name}<slot></slot></span> { since && <span class="px-3 text-green-500 italic capitalize">available since {since}</span> }
