---
const { name, since } = Astro.props;
---
<span class="bg-blue-100 border-b-2 border-b-indigo-500">{name && name}<slot></slot></span> { since && <span class="px-3 text-green-500 italic capitalize">available since {since}</span> }
