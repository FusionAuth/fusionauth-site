---
publish_date: 2026-04-21
title: Stop Being Your Customers' SSO Helpdesk
description: Enterprise SSO has always come with a tax — on your team's time, on your per-connection bill, or both. FusionAuth v1.65 cuts it.
authors: Andrew Hatfield
image: /img/blogs/tenant-manager-sso/tenant-manager-sso.png
categories: Product
tags: sso, enterprise, tenant manager, saml, oidc, b2b
excerpt_separator: "{/* more */}"
---

Enterprise SSO has always come with a tax — on your team's time, on your per-connection bill, or both. FusionAuth v1.65 cuts it.

{/* more */}

Here's a pattern I've seen repeated across B2B SaaS companies at every stage.

You close your first enterprise deal. It's proof you've built something enterprises actually want. Six weeks later, their IT admin emails about SSO. Your engineering team handles it. By "handles it," I mean a ticket, a back-and-forth, an admin session, and a misconfiguration that doesn't surface until someone tries to log in for the first time.

Three weeks. One customer.

Now you've got four more enterprise deals in the pipeline, and every one of them will follow the same story.

## The math breaks faster than you think

Close the deal. Get the SSO request. Handle the configuration. Find the misconfiguration in production. Fix it.

One customer: manageable. Five customers: a cost your engineering manager is quietly absorbing. Twenty customers: a line item your CFO is going to notice and your CTO is going to have to answer for.

What no one realizes in the beginning: this isn't a technical problem. Your product works. Your auth stack works. The problem is that the wrong team owns the process. Your engineers are doing IT work for your customers' companies because those customers have no self-service path. Every enterprise deal you close adds to a queue that doesn't self-clear.

That's the case you need to make internally — not "this is annoying," but "this is a cost that gets worse with every deal we close."

## There's also a pricing problem — depending on how you're solving it

If you've been evaluating other platforms to solve this, you've likely seen two pricing models in the market — and both have the same problem.

Some charge per SSO connection. $125 per connection per month is common. Twenty enterprise customers: $2,375 per month, just for the self-service SSO layer, before you've written a line of product code. That number grows with every deal you close.

Others cap enterprise connections at the plan level rather than charging per connection. Three connections on one tier, five on the next. Your sixth enterprise customer forces a jump to their top tier and a sales conversation. If you started on a lower-cost track and added enterprise buyers, you've already hit the forcing function — minimum pricing jumps of 4x or more, for the same MAU cap.

The mechanism differs. The outcome is the same: unpredictable costs that multiply as you scale.

Most teams accept this as a cost of doing enterprise business. You shouldn't, because it isn't.

## What FusionAuth v1.65 ships

Tenant Manager now includes a complete self-service SSO configuration flow.

Your enterprise customer's IT admin logs in, selects their identity provider type, fills in the form, and runs a built-in test before anything goes live. Pass: they enable it. Fail: they see exactly what went wrong, fix it, re-test, and enable it. Done — without emailing your team.

[SAML v2](/docs/lifecycle/authenticate-users/saml) and [OIDC](/docs/lifecycle/authenticate-users/identity-providers/overview-oidc) are both supported. For OIDC, the discovery endpoint auto-populates configuration from the issuer URL. Most modern providers — Okta, Entra, Google Workspace — resolve with a single field.

You keep control of the guardrails. You set which IdP types each tenant can access and define the linking strategies. Your customers work within those guardrails without pulling your team in.

## The test is what actually matters

The most common SSO escalation I've seen isn't a missing feature. It's a misconfiguration that looks right in the form but fails when someone tries to log in.

Every IdP configuration can now be tested against the real identity provider before it touches production. Pass or fail, clearly shown. If it fails, the error detail tells the tenant admin what's wrong — so they fix it before it reaches your queue.

Your team never even knows it happens because your customer handled it themselves.

This is the specific detail your CTO cares about: the problem isn't just removed from your team's queue. It's prevented at the source. There's a difference between "we handled it faster" and "it stopped happening." V1.65 is the second one.

## Two things that don't change regardless of which competitor you're evaluating

Self-service SSO in [Tenant Manager](/docs/lifecycle/manage-users/tenant-manager) closes the capability gap. But the platform advantages that made you evaluate FusionAuth in the first place are unchanged — and they matter more at enterprise scale.

FusionAuth [platform licensing](/feature-list) covers all SSO connections. No per-connection charge. No plan-tier forcing. The cost stays flat as your enterprise customer base grows. Against WorkOS, for 20 enterprise customers, that's $2,375 per month you're not paying.

And FusionAuth is the only commercial CIAM platform in this category with a [single-tenant deployment option](/articles/identity-basics/multi-tenancy-vs-single-tenant-idaas-solutions). [Auth0, WorkOS, Stytch, Descope](/guides/auth0-alternatives) — all multi-tenant cloud-only. If you're selling into financial services, healthcare, or government, or into EU markets where data sovereignty is a contractual requirement, isolated single-tenancy isn't a preference. It's the answer your competitors can't give. For the deals where that matters, it's the close.

## What to do now

If you're already on FusionAuth: Tenant Manager SSO is live in [v1.65](/docs/release-notes) from April 30th, 2026. Open Admin App settings, configure which IdP types your tenants can access, and point your enterprise customers to Tenant Manager. The queue clears itself from here.

If you're evaluating FusionAuth: ask to see the Tenant Manager SSO flow in your [demo](/contact). Your SE can run configure-to-test-to-enable end-to-end in under 15 minutes against a real IdP environment. That's the demo you bring back to your CTO — and it answers both questions they're going to ask: does it work, and does it take my team out of the loop?
