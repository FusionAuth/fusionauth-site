import APIField from '../../../../../components/api/APIField.astro';
import EnterprisePlanBlurbApi from '../../../_shared/_enterprise-plan-blurb-api.astro';

<APIField name="event.info.data" type="Object" since={props.sinceThreatDetection}>
  An object that can hold any information about the event that should be persisted.
</APIField>

<APIField name="event.info.deviceDescription" type="String" since={props.sinceThreatDetection}>
  The description of the device associated with the event.
</APIField>

<APIField name="event.info.deviceName" type="String" since={props.sinceThreatDetection}>
  The device name associated with the event.
</APIField>

<APIField name="event.info.deviceType" type="String" since={props.sinceThreatDetection}>
  The type of device associated with the event.
</APIField>

<APIField name="event.info.ipAddress" type="String" since={props.sinceIpAddress}>
  The source IP address of the event.
</APIField>

<APIField name="event.info.location.city" type="String" since={props.sinceThreatDetection}>
  The city where the event originated.
  <EnterprisePlanBlurbApi feature="event location data" />
</APIField>

<APIField name="event.info.location.country" type="String" since={props.sinceThreatDetection}>
  The country where the event originated.
  <EnterprisePlanBlurbApi feature="event location data" />
</APIField>

<APIField name="event.info.location.latitude" type="Double" since={props.sinceThreatDetection}>
  The latitude where the event originated.
  <EnterprisePlanBlurbApi feature="event location data" />
</APIField>

<APIField name="event.info.location.longitude" type="Double" since={props.sinceThreatDetection}>
  The longitude where the event originated.
  <EnterprisePlanBlurbApi feature="event location data" />
</APIField>

<APIField name="event.info.location.region" type="String" since={props.sinceThreatDetection}>
  The geographic location where the event originated.
  <EnterprisePlanBlurbApi feature="event location data" />
</APIField>

<APIField name="event.info.location.zipcode" type="String" since={props.sinceThreatDetection}>
  The zip code where the event originated.
  <EnterprisePlanBlurbApi feature="event location data" />
</APIField>

<APIField name="event.info.os" type="String" since={props.sinceThreatDetection}>
  The operating system associated with the event.
</APIField>

<APIField name="event.info.userAgent" type="String" since={props.sinceThreatDetection}>
  The user agent associated with the event.
</APIField>
