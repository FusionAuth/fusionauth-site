---
---
import InlineField from 'src/components/InlineField.astro';

This is a tenant scoped event.

{`
  if (!props.version || (props.version < 1.38)) {
    "Prior to version 1.37.0 this event was sent to all webhooks, and it was a webhook's responsibility to filter on the <InlineField>tenantId</InlineField> field."
  }
`}
