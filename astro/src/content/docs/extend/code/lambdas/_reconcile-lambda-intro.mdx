---
---
When {props.prefix} {props.identity_provider_name} identity provider is used to complete a federated login request, FusionAuth will use the [configured linking strategy](/docs/lifecycle/authenticate-users/identity-providers/#linking-strategies) {props.extra_reconcile !== "" ? props.extra_reconcile : "" }to reconcile the user. FusionAuth will attempt to match the user information returned from the {props.identity_provider_name} identity provider to an existing user account or create a new one.

You may optionally utilize a lambda to customize the user and user registration during the authentication event.

{props.extra_text !== "" ? <p>{props.extra_text}</p> : ""}

When you create a new lambda using the FusionAuth administrative user interface, you will be provided an empty function to implement.

