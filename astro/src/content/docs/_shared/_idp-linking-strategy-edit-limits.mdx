import AvailableSince from 'src/components/api/AvailableSince.astro';

<AvailableSince since="1.65.0" /> You can **only** change this value when the identity provider is **disabled**. To change the linking strategy for an identity provider that is already enabled, disable the provider, change the value, and then re-enable the provider.
