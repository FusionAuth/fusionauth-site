import Steps from 'src/components/Steps.astro';
import Breadcrumb from 'src/components/Breadcrumb.astro';
import InlineUIElement from 'src/components/InlineUIElement.astro';

To enable {props.identity_provider || "an Identity Provider"} for an Application:

<Steps>

1. Navigate to <Breadcrumb>Settings -> Identity Providers</Breadcrumb>.

1. Click the <InlineUIElement>Add provider</InlineUIElement> dropdown and select <Breadcrumb>{props.identity_provider}</Breadcrumb>. This will take you to the <Breadcrumb>Add {props.identity_provider}</Breadcrumb> screen.

1. {props.details_link ? (
      <>
        Fill out the required fields; your identity provider can provide most of these values. For a detailed description of each field, see the <a href={props.details_link}>documentation for {props.identity_provider}</a>.
      </>
    ) : (
      <>
        Fill out the required fields; your identity provider can provide most of these values. For more information about the fields required for each identity provider, see the <a href="/docs/lifecycle/authenticate-users/identity-providers/">documentation for your provider</a>.
      </>
    )}

1. In the <Breadcrumb>Applications</Breadcrumb> configuration section at the bottom of this panel, find your application name.

1. To dynamically create FusionAuth user accounts (with default roles) for all users who successfully authenticate using this provider, select <Breadcrumb>Create registration</Breadcrumb>. If you disable this option, you must manually register each user for this application before they may Sign in with {props.identity_provider}.

1. {props.login_message ? (
     props.login_message
   ) : (
     <>You should now see the <Breadcrumb>Sign in with {props.identity_provider}</Breadcrumb> button on the login page for the Pied Piper application.</>
   )}

</Steps>

<img 
  src={props.image || "/img/docs/lifecycle/authenticate-users/identity-providers/external-jwt/identity-provider-external-jwt-add.png"} 
  alt={`Add ${props.identity_provider || "an Identity Provider"}`} 
/>
