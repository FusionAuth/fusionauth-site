---
title: .NET API
description: QuickStart integration of .NET Web API with FusionAuth.
htmlTitle: FusionAuth QuickStart for .NET API
---

import Aside from 'src/components/Aside.astro';
import LocalCode from 'src/components/LocalCode.astro';
import QuickstartTshirtCTA from 'src/components/quickstarts/QuickstartTshirtCTA.astro'

This QuickStart secures an ASP.NET Web API resource server with FusionAuth using a preconfigured, local version of FusionAuth in the following steps:

1. Run FusionAuth with Docker.
1. Create a new ASP.NET Web API application.
1. Add JWT-based authentication, model objects, and controllers for two protected endpoints.
1. Get a token and call the protected endpoints.

You'll be building the API for ChangeBank, a bank that converts dollars into coins. It has two endpoints:

* `make-change`: Call this with a `total` amount to get back the number of nickels and pennies needed to make change. The `customer` and `teller` roles can both call this endpoint.
* `panic`: Tellers call this endpoint to alert the police in case of an incident. Only the `teller` role can call this endpoint.

Both endpoints require a valid JSON Web Token (JWT) in the `Authorization` header or in a cookie named `app.at`, and the JWT's `roles` claim must contain the role required by the endpoint.

## Prerequisites

This QuickStart requires:

* [.NET 7 SDK](https://dotnet.microsoft.com/en-us/download/dotnet/7.0) — this QuickStart's packages are pinned to .NET 7. Other compatible .NET versions may work but haven't been tested.
* [Docker](https://docs.docker.com/get-started/get-docker/), with [Docker Compose](https://docs.docker.com/compose/)
* Optionally, [Visual Studio](https://visualstudio.microsoft.com/) or another IDE to make it easier to work with .NET

## Install and run FusionAuth

To begin, clone the [GitHub repository for this Quickstart](https://github.com/FusionAuth/fusionauth-quickstart-dotnet-api) to your local machine:

```bash
git clone https://github.com/FusionAuth/fusionauth-quickstart-dotnet-api
cd fusionauth-quickstart-dotnet-api
```

Start a local instance of FusionAuth:

```bash
docker compose up -d
```

<Aside type="note">
During the Docker Compose process, the included `kickstart/kickstart.json` file uses the FusionAuth API to configure FusionAuth to a working state, including:

* Generating an API key and configuring CORS
* Creating the ChangeBank application, with a client Id of `e9fdb985-9173-4e01-9d73-ac2d60d1dc8e`
* Creating an admin user (`admin@example.com`, password `password`)
* Creating a teller user (`teller@example.com`, password `password`) with the `teller` role
* Creating a customer user (`customer@example.com`, password `password`) with the `customer` role
</Aside>

Once Docker Compose completes, open a browser window and navigate to the admin UI of your FusionAuth instance at [http://localhost:9011/admin](http://localhost:9011/admin). Log in with the admin credentials above to confirm FusionAuth is running and configured.

<Aside type="note">
If you ever want to reset FusionAuth to this initial state, delete the volumes created by Docker Compose with `docker compose down -v`, then run `docker compose up -d` again.
</Aside>

## Create the ASP.NET Web API application

While this section builds a simple API from scratch, you can apply the same configuration to secure an existing ASP.NET API with FusionAuth.

<Aside type="note">
If you just want to run the finished application, there's a completed version in the `complete-application` directory:

```shell
cd complete-application
dotnet run
```

Skip ahead to [Get a token](#get-a-token) to try it.
</Aside>

### Initialize the application

From the repository root, generate a new ASP.NET Web API project:

```shell
dotnet new webapi -o your-api && cd your-api
```

The template scaffolds a sample `WeatherForecastController` and `WeatherForecast` model that you don't need for this application. Remove them so you don't end up with a stray, unauthenticated endpoint alongside the secured ones you're about to build:

```shell
rm Controllers/WeatherForecastController.cs WeatherForecast.cs
```

### Add dependencies

This app uses the standard [.NET JWTBearer package](https://www.nuget.org/packages/Microsoft.AspNetCore.Authentication.JWTBearer), which simplifies extracting and validating JWT tokens from FusionAuth. Install it, along with supporting libraries:

```shell
dotnet add package Microsoft.AspNetCore.Authentication.JwtBearer -v 7.0.14
dotnet add package Microsoft.AspNetCore.OpenApi -v 7.0.10
dotnet add package System.IdentityModel.Tokens.Jwt -v 7.0.3
dotnet add package Microsoft.IdentityModel.Protocols.OpenIdConnect -v 7.0.3
```

### Configure settings

Replace your `appsettings.json` file with settings specific to the FusionAuth Issuer and Client Id (Audience):

<LocalCode src="fusionauth-quickstart-dotnet-api-main/complete-application/appsettings.json" lang="json" />

Replace the code in the `appsettings.Development.json` file with the following. This file is used when you run the application in development mode, and enables some useful development options — mainly that HTTPS will not be required for the Issuer (FusionAuth) server. In production, you must use HTTPS for the Issuer server:

<LocalCode src="fusionauth-quickstart-dotnet-api-main/complete-application/appsettings.Development.json" lang="json" />

### Add JWT authentication

Update `Program.cs` to configure ASP.NET authentication to use JWT tokens. Replace the code in `Program.cs` with the following:

<LocalCode src="fusionauth-quickstart-dotnet-api-main/complete-application/Program.cs" lang="csharp" />

This configures the authentication services to use JWT bearer tokens via the `.AddJwtBearer()` method. The Issuer and Audience settings used by the JWT verification code come from the `Authentication` section in `appsettings.json`. The `options.Events` section configures JWT bearer authentication to also read the JWT from the `app.at` cookie via the `OnMessageReceived` event — by default, JWT bearer authentication only reads the JWT from the `Authorization` header, so this lets the token be passed in either place.

### Add model objects

Create a `Models` folder:

```shell
mkdir Models
```

In the `Models` folder, create a new class `PanicResponse.cs`. This holds the message returned by a successful `POST` call to `/panic`:

```shell
touch Models/PanicResponse.cs
```

<LocalCode src="fusionauth-quickstart-dotnet-api-main/complete-application/Models/PanicResponse.cs" lang="csharp" />

Next, create `Models/Change.cs`, which holds the breakdown from making change — a `double total` and integer `nickels` and `pennies`:

```shell
touch Models/Change.cs
```

<LocalCode src="fusionauth-quickstart-dotnet-api-main/complete-application/Models/Change.cs" lang="csharp" />

### Add controllers

In the `Controllers` folder, add a new class `MakeChangeController.cs`. Its `Get` method converts the `total` query parameter into a `double`, then breaks it into `nickels` and `pennies`:

```shell
touch Controllers/MakeChangeController.cs
```

<LocalCode src="fusionauth-quickstart-dotnet-api-main/complete-application/Controllers/MakeChangeController.cs" lang="csharp" />

Note the `[Authorize(Roles = "teller,customer")]` attribute, which adds ASP.NET role-based authorization to the route. The ASP.NET JWT Bearer package automatically reads the roles claimed in the user token and adds them to the user object. For example, a decoded JWT for a `teller` looks like this:

```json
{
  "aud": "e9fdb985-9173-4e01-9d73-ac2d60d1dc8e",
  "exp": 1689289585,
  "iat": 1689285985,
  "iss": "http://localhost:9011",
  "sub": "00000000-0000-0000-0000-111111111111",
  "jti": "ebaa4184-2320-47dd-925b-2e18756c635f",
  "authenticationType": "PASSWORD",
  "email": "teller@example.com",
  "email_verified": true,
  "applicationId": "e9fdb985-9173-4e01-9d73-ac2d60d1dc8e",
  "roles": [
    "teller"
  ],
  "auth_time": 1689285985,
  "tid": "d7d09513-a3f5-401c-9685-34ab6c552453"
}
```

ASP.NET reads the `roles` field, adds those roles to the user, and tests the user against the roles required by the `Authorize` attribute.

Now add a controller for tellers to call in an emergency. In the `Controllers` folder, add a class `PanicController.cs`. It listens on `/panic` for a `POST` request and returns a response indicating that the police were called. Its attributes are the same as `MakeChangeController.cs`, except it's restricted to the `teller` role and marked `[HttpPost]`:

```shell
touch Controllers/PanicController.cs
```

<LocalCode src="fusionauth-quickstart-dotnet-api-main/complete-application/Controllers/PanicController.cs" lang="csharp" />

## Run the API

Start the API resource server. In a terminal window, navigate to the root directory of your application (`your-api`) and run:

```shell
dotnet run
```

This builds and runs the project, which listens on port 5001.

<Aside type="note">
Leave the server running in this terminal and open a new terminal window for the remaining commands.
</Aside>

### Get a token

There are [several ways to acquire a token](/docs/lifecycle/authenticate-users/oauth/) in FusionAuth, but this QuickStart uses the [Login API](/docs/apis/login) to keep things simple.

First, try the requests as `teller@example.com`, who has the `teller` role and can call both `/make-change` and `/panic`.

Acquire an access token for `teller@example.com`:

```shell
curl --location 'http://localhost:9011/api/login' \
--header 'Authorization: this_really_should_be_a_long_random_alphanumeric_value_but_this_still_works' \
--header 'Content-Type: application/json' \
--data-raw '{
  "loginId": "teller@example.com",
  "password": "password",
  "applicationId": "e9fdb985-9173-4e01-9d73-ac2d60d1dc8e"
}'
```

Your response should look like this. Grab the `token` field (it begins with `ey`):

```json
{
   "token": "eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6InVOYl9iQzFySHZZTnZMc285VzRkOEprZkxLWSJ9..WLzI9hSsCDn3ZoHKA9gaifkd6ASjT03JUmROGFZaezz9xfVbO3quJXEpUpI3poLozYxVcj2hrxKpNT9b7Sp16CUahev5tM0-4_FaYlmUEoMZBKo2JRSH8hg-qVDvnpeu8nL6FXxJII0IK4FNVwrQVFmAz99ZCf7m5xruQSziXPrfDYSU-3OZJ3SRuvD8bMopSiyRvZLx6YjWfBsvGSmMXeh_8vHG5fVkq5w1IkaDdugHnivtJIivHuCfl38kQBgw9rAqJLJoKRHHW0Ha7vHIcS6OCWWMDIIVspLyQNcLC16pL9Nss_5v9HMofow1OvQ9sUSMrbbkipjKq2peSjG7qA",
   "tokenExpirationInstant": 1689353059670,
   "user": {
      ...
   }
}
```

### Call the protected endpoints

<Aside type="note">
The code reads the token from either a cookie named `app.at` or the `Authorization` header, so you can use whichever method you prefer in the commands below.
</Aside>

Call `/make-change` with a query parameter `total=5.12`, using the `Authorization` header:

```shell
curl --location 'http://localhost:5001/makeChange?total=5.12' \
--header 'Authorization: Bearer <your_token>'
```

Or use a cookie instead:

```shell
curl --location 'http://localhost:5001/makeChange?total=5.12' \
--cookie 'app.at=<your_token>'
```

Your response should look like this:

```json
{
  "total": 5.12,
  "nickels": 102,
  "pennies": 2
}
```

You're authorized, success! Try the request again without the `Authorization` header, or with an invalid token, and you'll be denied access.

Now call `/panic`, since you're in trouble:

```shell
curl --location --request POST 'http://localhost:5001/panic' \
--header 'Authorization: Bearer <your_token>'
```

Or using a cookie:

```shell
curl --location --request POST 'http://localhost:5001/panic' \
--cookie 'app.at=<your_token>'
```

This is a `POST`, not a `GET`, because emergency calls should be non-idempotent. Your response should look like this:

```json
{
  "message": "We've called the police!"
}
```

Nice, help is on the way!

Now try the same requests as `customer@example.com`, who has the `customer` role. Acquire a token for `customer@example.com`:

```shell
curl --location 'http://localhost:9011/api/login' \
--header 'Authorization: this_really_should_be_a_long_random_alphanumeric_value_but_this_still_works' \
--header 'Content-Type: application/json' \
--data-raw '{
  "loginId": "customer@example.com",
  "password": "password",
  "applicationId": "e9fdb985-9173-4e01-9d73-ac2d60d1dc8e"
}'
```

Your response should look like this. Grab the `token` field:

```json
{
  "token": "eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6InVOYl9iQzFySHZZTnZMc285VzRkOEprZkxLWSJ9..T1bELQ6a_ItOS0_YYpvqhIVknVMNeamcoC7BWnPjg2lgA9XpCmFA2mVnycoeuz-mSOHbp2cCoauP5opxehBR2lCn4Sz0If6PqgJgXKEpxh5pAxCPt91UyfjH8hGDqE3rDh7E4Hqn7mb-dFFwdfX7CMdKvC3dppMbXAGCZTl0LizApw5KIG9Wmt670339pSf5lzD38P9WAG5Wr7fAmVrIJPVu6yv2FoR-pMYD2lnAF63HWKknrWB-khmhr9ZKRLXKhP1UK-ThY1FSnmpp8eNblsBqCxf6WaYxYkdp5_F2e56M4sQwHzrg4P9tZGVCmMri4dShF3Ck7OGa7hel-iIPew",
  "tokenExpirationInstant": 1689354123118,
  "user": {
    ...
  }
}
```

Call `/make-change` with `total=3.24`:

```shell
curl --location 'http://localhost:5001/makeChange?total=3.24' \
--header 'Authorization: Bearer <your_token>'
```

Or using a cookie:

```shell
curl --location 'http://localhost:5001/makeChange?total=3.24' \
--cookie 'app.at=<your_token>'
```

Your response should look like this:

```json
{
  "total": 3.24,
  "nickels": 64,
  "pennies": 4
}
```

So far so good. Now call `/panic`. Add the `-i` flag to see the response headers:

```shell
curl -i --request POST 'http://localhost:5001/panic' \
--header 'Authorization: Bearer <your_token>'
```

Or using a cookie:

```shell
curl -i --request POST 'http://localhost:5001/panic' \
--cookie 'app.at=<your_token>'
```

Since `customer@example.com` doesn't have the `teller` role, you'll get a `403 Forbidden` response:

```shell
HTTP/1.1 403 Forbidden
Content-Length: 0
Date: Wed, 11 Oct 2023 08:29:09 GMT
Server: Kestrel
```

Uh oh, customers aren't allowed to call the police. Notice this returns `403 Forbidden` rather than `401 Unauthorized`, meaning the user was authenticated, but forbidden from accessing this particular resource. Your secured resource server is working as designed.

<QuickstartTshirtCTA/>

## Next steps

This is a quick introduction to using FusionAuth to secure an API. To learn more about integrating FusionAuth with your product, see [Get Started](/docs/get-started).

For a production API, consider:

* Using the [Authorization Code grant](/docs/lifecycle/authenticate-users/oauth/#example-authorization-code-grant) instead of the Login API, which keeps credentials within FusionAuth. You can customize the [hosted login pages](/docs/customize/look-and-feel/).
* Using the [Client Credentials grant](/docs/lifecycle/authenticate-users/oauth/#example-client-credentials-grant) to generate a token when calling the API from another service.
* Customizing [token expiration times and policies](/docs/lifecycle/authenticate-users/oauth/#configure-application-oauth-settings) in FusionAuth.
* Making sure you know how to [securely consume a token](/articles/tokens/building-a-secure-jwt#consuming-a-jwt).
* Securing your API [using an API gateway](/docs/extend/examples/api-gateways/) rather than at the framework layer.

FusionAuth has many other features (including [Social Login](/docs/lifecycle/authenticate-users/identity-providers/#social-identity-providers), [Single Sign-On](/docs/lifecycle/authenticate-users/single-sign-on), [Passwordless](/docs/lifecycle/authenticate-users/passwordless/webauthn-passkeys), [Multi-factor Authentication](/docs/lifecycle/authenticate-users/multi-factor-authentication)), most of which are free. Some require additional licensing. For details, see the [pricing page](/pricing).

You can ask general questions in [the FusionAuth forum](/community/forum).

## Troubleshooting

* I get `Failed to connect to localhost port 9011: Couldn't connect to server` when I call the Login API.

  Ensure FusionAuth is running in the Docker container. You should be able to log in as the admin user, `admin@example.com` with a password of `password`, at [http://localhost:9011/admin](http://localhost:9011/admin).

* The `/panic` endpoint doesn't work when I call it.

  Make sure you're making a `POST` call and using a token with the `teller` role.

* It still doesn't work.

  Pull down a complete running application and compare what's different:

  ```shell
  git clone https://github.com/FusionAuth/fusionauth-quickstart-dotnet-api.git
  cd fusionauth-quickstart-dotnet-api
  docker compose up -d
  cd complete-application
  dotnet run
  ```
