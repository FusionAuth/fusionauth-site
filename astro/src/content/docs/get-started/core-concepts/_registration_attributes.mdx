import APIBlock from 'src/components/api/APIBlock.astro';
import APIField from 'src/components/api/APIField.astro';

<APIBlock>
  <APIField name="Username" optional>
    The username of the User for this registration only.
  </APIField>
  <APIField name="Languages" optional>
    A list of locales that give, in order, the User's preferred languages. These are important for email templates and other localizable text. See [Locales](/docs/reference/data-types#locales).
  </APIField>
  <APIField name="Timezone" optional>
    The User's preferred timezone for this registration. 
  </APIField>
  <APIField name="Roles" optional>
    The roles that the User has for this registration.
  </APIField>
</APIBlock>
