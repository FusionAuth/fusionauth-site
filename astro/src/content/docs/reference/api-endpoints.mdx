---
title: API Endpoints Guarded by API Keys
description: A complete reference of all Fusionauth REST API Endpoints that leverage API Keys with their supported HTTP methods.
---
import APIAuthenticationIcon from 'src/components/api/APIAuthenticationIcon.astro';
import APIEndpointList from 'src/content/docs/reference/_api-endpoint-list.astro';

FusionAuth exposes a REST API. This page lists API endpoints that leverage API Keys along with the supported HTTP methods.

For full request and response documentation, see the [API reference](/docs/apis/).

## Authentication

The **Auth** column shows the API key scope required for each endpoint:

<ul class="not-prose flex flex-col gap-1 list-disc pl-5">
  <li class="flex items-center gap-2"><APIAuthenticationIcon type="api-key" classList={['!my-0']} /> [API Key Authentication](/docs/apis/authentication#api-key-authentication) — a tenant-scoped or global API key may be used.</li>
  <li class="flex items-center gap-2"><APIAuthenticationIcon type="global-api-key" classList={['!my-0']} /> [Global API Key Authentication](/docs/apis/authentication#global-api-key-authentication) — only a global API key may be used.</li>
</ul>

## Endpoint List

<APIEndpointList />
