import APIField from 'src/components/api/APIField.astro';
import Aside from 'src/components/Aside.astro';

<APIField name={props.plural ? 'loginIdTypes' : 'loginIdType'} type={props.plural ? 'Array<String>' : 'String'} defaults={props.plural ? '[email, username]' : 'email or username if email not found'} optional since={props.since || '1.59.0'}>
  The identity type{props.plural ? 's' : ''} that FusionAuth will compare the **loginId** to. Can be one{props.plural ? ' or more' : ''} of the following:

  * `email`
  * `phoneNumber`
  * `username`

  { props.plural &&
    <Aside>
      The order is significant. If `["email", "username"]` is supplied with a **loginId** value of `terry@example.com`, then a user with `terry@example.com` as their email address will match first before any user with `terry@example.com` as their username.
    </Aside>
  }
</APIField>
