
{/*  base_field_name is "domain" for a single CIMD domain, or "domains[x]" when rendering a list. */}

<APIBlock>
  <APIField name={ props.base_field_name + ".authorizedResourceUris" } type="Array<String>">
    The resource URIs that clients registered on this CIMD domain may request using the [RFC 8707](https://datatracker.ietf.org/doc/html/rfc8707) `resource` parameter.

    When this list is empty, the `resource` parameter is silently ignored for this domain's clients.
  </APIField>
  <APIField name={ props.base_field_name + ".consentMode" } type="String">
    Controls whether a user is prompted to consent to the OAuth scopes requested by this domain's clients.

    The possible values are:

    * `AlwaysPrompt` - Always prompt the user for consent.
    * `RememberDecision` - Remember previous consents; only prompt if the choice expires or if the requested or required scopes have changed. The duration of this persisted choice is controlled by the Tenant's **externalIdentifierConfiguration.rememberOAuthScopeConsentChoiceTimeToLiveInSeconds** value.

    `NeverPrompt` is not a valid value for a CIMD domain. CIMD clients are always treated as third-party clients, so consent can never be granted implicitly.
  </APIField>
  <APIField name={ props.base_field_name + ".data" } type="Object">
    An object that can hold any information about the CIMD domain that should be persisted.
  </APIField>
  <APIField name={ props.base_field_name + ".debug" } type="Boolean">
    Whether FusionAuth will write a debug Event Log during the CIMD client registration process. This is useful for diagnosing why a client's metadata document was rejected.
  </APIField>
  <APIField name={ props.base_field_name + ".domain" } type="String">
    The normalized origin that CIMD clients register from, in the form `scheme://host[:port]`, or the literal `*` to accept registrations from any origin.

    FusionAuth uses this value to validate the `client_id` URL presented on an OAuth request.
  </APIField>
  <APIField name={ props.base_field_name + ".gracePeriodInSeconds" } type="Integer">
    How long, in seconds, FusionAuth will continue to honor an expired cached metadata document when a re-fetch of that document fails.
  </APIField>
  <APIField name={ props.base_field_name + ".id" } type="UUID">
    The unique Id of the CIMD domain.
  </APIField>
  <APIField name={ props.base_field_name + ".insertInstant" } type="Long">
    The [instant](/docs/reference/data-types#instants) that the CIMD domain was added to the FusionAuth database.
  </APIField>
  <APIField name={ props.base_field_name + ".jwtPopulateLambdaId" } type="UUID">
    The unique Id of the lambda used to populate the claims of the access token and Id token issued to this domain's clients.
  </APIField>
  <APIField name={ props.base_field_name + ".lastUpdateInstant" } type="Long">
    The [instant](/docs/reference/data-types#instants) that the CIMD domain was last updated in the FusionAuth database.
  </APIField>
  <APIField name={ props.base_field_name + ".maxCacheTtlInSeconds" } type="Integer">
    The ceiling, in seconds, applied to the `Cache-Control: max-age` value returned when fetching a client's metadata document. When the document specifies a smaller value, that smaller value is used. The `Expires` header is not honored.

    A non-configurable 15 minute floor is always applied, so a value lower than `900` has no additional effect.
  </APIField>
  <APIField name={ props.base_field_name + ".name" } type="String">
    The name of the CIMD domain, used to identify it in the FusionAuth admin UI. When not provided on create, this defaults to the value of **{props.base_field_name + ".domain"}**.
  </APIField>
  <APIField name={ props.base_field_name + ".providedScopePolicy.address.enabled" } type="Boolean">
    Whether the `address` OAuth scope provided by FusionAuth is enabled for this CIMD domain. A disabled scope is treated as an unknown scope when requested.
  </APIField>
  <APIField name={ props.base_field_name + ".providedScopePolicy.address.required" } type="Boolean">
    Whether consent to the `address` OAuth scope provided by FusionAuth is required for this CIMD domain when present on the OAuth request.
  </APIField>
  <APIField name={ props.base_field_name + ".providedScopePolicy.email.enabled" } type="Boolean">
    Whether the `email` OAuth scope provided by FusionAuth is enabled for this CIMD domain. A disabled scope is treated as an unknown scope when requested.
  </APIField>
  <APIField name={ props.base_field_name + ".providedScopePolicy.email.required" } type="Boolean">
    Whether consent to the `email` OAuth scope provided by FusionAuth is required for this CIMD domain when present on the OAuth request.
  </APIField>
  <APIField name={ props.base_field_name + ".providedScopePolicy.phone.enabled" } type="Boolean">
    Whether the `phone` OAuth scope provided by FusionAuth is enabled for this CIMD domain. A disabled scope is treated as an unknown scope when requested.
  </APIField>
  <APIField name={ props.base_field_name + ".providedScopePolicy.phone.required" } type="Boolean">
    Whether consent to the `phone` OAuth scope provided by FusionAuth is required for this CIMD domain when present on the OAuth request.
  </APIField>
  <APIField name={ props.base_field_name + ".providedScopePolicy.profile.enabled" } type="Boolean">
    Whether the `profile` OAuth scope provided by FusionAuth is enabled for this CIMD domain. A disabled scope is treated as an unknown scope when requested.
  </APIField>
  <APIField name={ props.base_field_name + ".providedScopePolicy.profile.required" } type="Boolean">
    Whether consent to the `profile` OAuth scope provided by FusionAuth is required for this CIMD domain when present on the OAuth request.
  </APIField>
  <APIField name={ props.base_field_name + ".registrationLambdaId" } type="UUID">
    The unique Id of the [CIMD Client Registration lambda](/docs/extend/code/lambdas) used to accept, reject, or modify a client registration for this domain. When this is not set, no registration lambda runs.
  </APIField>
  <APIField name={ props.base_field_name + ".scopes" } type="Array">
    The custom OAuth scopes defined on this CIMD domain. Manage these using the [CIMD Domain OAuth Scope APIs](/docs/apis/cimd-domain-scopes).
  </APIField>
  <APIField name={ props.base_field_name + ".scopes[x].data" } type="Object">
    An object that can hold any information about the OAuth scope that should be persisted.
  </APIField>
  <APIField name={ props.base_field_name + ".scopes[x].defaultConsentDetail" } type="String">
    The default detail to display on the OAuth consent screen if one cannot be found in the theme.
  </APIField>
  <APIField name={ props.base_field_name + ".scopes[x].defaultConsentMessage" } type="String">
    The default message to display on the OAuth consent screen if one cannot be found in the theme.
  </APIField>
  <APIField name={ props.base_field_name + ".scopes[x].description" } type="String">
    A description of the OAuth scope for internal use.
  </APIField>
  <APIField name={ props.base_field_name + ".scopes[x].id" } type="UUID">
    The unique Id of the OAuth scope.
  </APIField>
  <APIField name={ props.base_field_name + ".scopes[x].insertInstant" } type="Long">
    The [instant](/docs/reference/data-types#instants) that the OAuth scope was added to the FusionAuth database.
  </APIField>
  <APIField name={ props.base_field_name + ".scopes[x].lastUpdateInstant" } type="Long">
    The [instant](/docs/reference/data-types#instants) that the OAuth scope was updated in the FusionAuth database.
  </APIField>
  <APIField name={ props.base_field_name + ".scopes[x].name" } type="String">
    The name of the OAuth scope. This is the value that will be used to request the scope in OAuth workflows.
  </APIField>
  <APIField name={ props.base_field_name + ".scopes[x].required" } type="Boolean">
    Determines if the OAuth scope is required when requested in an OAuth workflow.
  </APIField>
  <APIField name={ props.base_field_name + ".tenantId" } type="UUID">
    The unique Id of the Tenant that this CIMD domain belongs to.
  </APIField>
</APIBlock>
