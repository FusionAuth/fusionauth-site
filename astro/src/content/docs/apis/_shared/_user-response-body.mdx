import UserResponseBodyBase from 'src/content/docs/apis/_shared/_user-response-body-base.mdx';
import APIBlock from 'src/components/api/APIBlock.astro';
import JSON from 'src/components/JSON.astro';
import If from 'src/components/If.astro';

{/*  Share search, single response fields */}

<APIBlock>
  <UserResponseBodyBase base_field_name="user" {...props} />
</APIBlock>

<If condition={props.login_response}>
  <JSON title="Example Response JSON" src="users/login-response.json" />
</If>

<If condition={props.passwordless_login_response}>
  <JSON title="Example Response JSON" src="users/passwordless-login-response.json" />
</If>

<If condition={props.create_user}>
  <JSON title="Example Response JSON" src="users/create-response.json" />
</If>

<If condition={!(props.login_response || props.passwordless_login_response || props.create_user)}>
  <JSON title="Example Response JSON" src="users/response.json" />
</If>

