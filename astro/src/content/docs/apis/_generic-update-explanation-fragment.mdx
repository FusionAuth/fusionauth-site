This API is used to update an existing {props.capitalized_object_name}.

{props.extra_id_verbiage && <span>You must specify the Id of the {props.capitalized_object_name} you are updating on the URI.</span> }

{props.extra_id_verbiage && <span>{props.extra_id_verbiage}</span>}

You must specify all of the properties of the {props.capitalized_object_name} when calling this API with the `PUT` HTTP method. When used with `PUT`, this API doesn't merge the existing {props.capitalized_object_name} and your new data. It replaces the existing {props.capitalized_object_name} with your new data.

Utilize the `PATCH` HTTP method to send specific changes to merge into an existing {props.capitalized_object_name}.

