import APIBlock from 'src/components/api/APIBlock.astro';
import APIField from 'src/components/api/APIField.astro';
import JSON from 'src/components/JSON.astro';

#### Response Body

<APIBlock>
  <APIField name="userActionReasons" type="Array">
    The list of User Action Reason objects.
  </APIField>
  <APIField name="userActionReasons[x].code" type="String">
    A short code for the User Action Reason.
  </APIField>
  <APIField name="userActionReasons[x].id" type="UUID">
    The Id of the User Action Reason.
  </APIField>
  <APIField name="userActionReasons[x].localizedTexts" type="Map<Locale, String>">
    A mapping of localized text for the User Action Reason. The key is the Locale and the value is the localized text.
  </APIField>
  <APIField name="userActionReasons[x].text" type="String">
    The text of the User Action Reason.
  </APIField>
</APIBlock>

<JSON title="Example Response JSON for all the User Action Reasons" src="user-action-reasons/responses.json" />
