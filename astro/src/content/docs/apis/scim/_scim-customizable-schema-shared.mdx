import ScimFieldsNote from 'src/content/docs/apis/scim/_scim-fields-note.mdx';

<ScimFieldsNote />

The following is an example of a typical SCIM {props.resource_type} {props.action} body. However, your incoming request <a href={'/docs/extend/code/lambdas/' + (props.lambda_file_name || '')}>lambda</a> must map these values the FusionAuth {props.fusionauth_type}. A <a href={'/docs/extend/code/lambdas/' + (props.lambda_file_name || '') + '#default-lambda'}>default lambda</a> is provided to handle a typical {props.action} that you may modify if necessary.

This example is taken from <a href={ "https://datatracker.ietf.org/doc/html/rfc7643#section-" + props.rfc_section }>RFC 7643 Section #{props.rfc_section}</a>.
