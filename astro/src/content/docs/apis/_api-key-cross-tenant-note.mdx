Tenant-scoped keys can retrieve configuration for FusionAuth entities such as lambdas that may be shared between tenants. Limit the API key by specifying required endpoints and permissions as well as the tenant.
