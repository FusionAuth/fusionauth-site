import DefaultSMTPConfigurationProperties from 'src/content/docs/_shared/default-smtp-configuration-properties.mdx';
import APIField from 'src/components/api/APIField.astro';
import EnterprisePlanBlurbApi from 'src/content/docs/_shared/_enterprise-plan-blurb-api.astro';
import InlineField from 'src/components/InlineField.astro';

  <APIField name={props.base_field_name + '.phoneConfiguration.forgotPasswordTemplateId'} type="UUID" since="1.59.0">
    The Id of the Message Template that is used when sending a user a forgot password message. {props.application_phone_config_override_text}
  </APIField>

  <APIField name={props.base_field_name + '.phoneConfiguration.identityUpdateTemplateId'} type="UUID" since="1.59.0">
    The Id of the Message Template used to send a message to a user when their phone number has been updated. The message will be sent to both their new and old phone numbers. {props.application_phone_config_override_text}
  </APIField>

  <APIField name={ props.base_field_name + ".phoneConfiguration.implicitPhoneVerificationAllowed" } optional type="Boolean" defaults="true" since="1.59.0" renderif={props.is_tenant}>
    When set to true, this allows a phone number to be verified as a result of completing a similar phone based workflow such as change password. When set to false, the user must explicitly complete the phone verification workflow even if the user has already completed a similar phone workflow such as change password.
  </APIField>

  <APIField name={props.base_field_name + '.phoneConfiguration.loginIdInUseOnCreateTemplateId'} type="UUID" since="1.59.0">
    The Id of the Message Template used to send a message to a user when another user attempts to create an account with their login Id. {props.application_phone_config_override_text}

    {props.show_feature_blurb && <EnterprisePlanBlurbApi feature="advanced threat detection messages" /> }
  </APIField>

  <APIField name={props.base_field_name + '.phoneConfiguration.loginIdInUseOnUpdateTemplateId'} type="UUID" since="1.59.0">
    The Id of the Message Template used to send a message to a user when another user attempts to update an existing account to use their login Id. {props.application_phone_config_override_text}

    {props.show_feature_blurb && <EnterprisePlanBlurbApi feature="advanced threat detection messages" /> }
  </APIField>

  <APIField name={props.base_field_name + '.phoneConfiguration.loginNewDeviceTemplateId'} type="UUID" since="1.59.0">
    The Id of the Message Template used to send a message to a user when they log in on a new device. {props.application_phone_config_override_text}

    {props.show_feature_blurb && <EnterprisePlanBlurbApi feature="advanced threat detection messages" /> }
  </APIField>

  <APIField name={props.base_field_name + '.phoneConfiguration.loginSuspiciousTemplateId'} type="UUID" since="1.59.0">
    The Id of the Message Template used to send a message to a user when a suspicious login using their login Id occurs. {props.application_phone_config_override_text}

    {props.show_feature_blurb && <EnterprisePlanBlurbApi feature="advanced threat detection messages" /> }
  </APIField>

  <APIField name={props.base_field_name + '.phoneConfiguration.messengerId'} type="UUID" since="1.59.0" renderif={props.is_tenant}>
    The messenger that is used to deliver SMS messages for phone number verification and passwordless logins.
  </APIField>

  <APIField name={props.base_field_name + '.phoneConfiguration.passwordlessTemplateId'} type="UUID" since="1.59.0">
    The Id of the Passwordless Message Template, sent to users when they start a passwordless login. {props.application_phone_config_override_text}
  </APIField>

  <APIField name={props.base_field_name + '.phoneConfiguration.passwordResetSuccessTemplateId'} type="UUID" since="1.59.0">
    The Id of the Message Template used to send a message to a user when they have completed a 'forgot password' workflow and their password has been reset. {props.application_phone_config_override_text}

    {props.show_feature_blurb && <EnterprisePlanBlurbApi feature="advanced threat detection messages" /> }
  </APIField>

  <APIField name={props.base_field_name + '.phoneConfiguration.passwordUpdateTemplateId'} type="UUID" since="1.59.0">
    The Id of the Message Template used to send a message to a user when their password has been updated. {props.application_phone_config_override_text}

    {props.show_feature_blurb && <EnterprisePlanBlurbApi feature="advanced threat detection messages" /> }
  </APIField>

  <APIField name={props.base_field_name + '.phoneConfiguration.setPasswordTemplateId'} type="UUID" optional since="1.59.0">
    The Id of the SMS Message Template used when a user must set their password manually after their account was created for them (by an admin, for example). {props.application_phone_config_override_text}
  </APIField>

  <APIField name={props.base_field_name + '.phoneConfiguration.twoFactorMethodRemoveTemplateId'} type="UUID" since="1.59.0">
    The Id of the Message Template used to send a message to a user when a MFA method has been added to their account. {props.application_phone_config_override_text}

    {props.show_feature_blurb && <EnterprisePlanBlurbApi feature="advanced threat detection messages" /> }
  </APIField>

  <APIField name={props.base_field_name + '.phoneConfiguration.twoFactorMethodAddTemplateId'} type="UUID" since="1.59.0">
    The Id of the Message Template used to send a message to a user when a MFA method has been removed from their account. {props.application_phone_config_override_text}

    {props.show_feature_blurb && <EnterprisePlanBlurbApi feature="advanced threat detection messages" /> }
  </APIField>

  <APIField name={ props.base_field_name + ".phoneConfiguration.unverified.allowPhoneNumberChangeWhenGated" } type="Boolean" optional defaults="false" since="1.59.0" renderif={props.is_tenant}>
    When this value is set to `true`, the user is allowed to change their phone number when they are gated because they haven't verified their phone number.
  </APIField>

  <APIField name={props.base_field_name + '.phoneConfiguration.unverified.behavior'} type="String" since="1.59.0" renderif={props.is_tenant}>
    The desired behavior during login for a user that does not have a verified phone number. The possible values are:

    * `Allow` - the user will be allowed to complete login.
    * `Gated` - verification is required before a user can complete login. The use of this value will require a paid plan.
  </APIField>

  <APIField name={props.base_field_name + '.phoneConfiguration.verificationCompleteTemplateId'} type="UUID" since="1.59.0">
    The Id of the Message Template used to notify a user that their phone number has been verified. {props.application_phone_config_override_text}
  </APIField>

  <APIField name={props.base_field_name + '.phoneConfiguration.verificationStrategy'} type="String" since="1.59.0" renderif={props.is_tenant}>
    The process by which the user will verify their phone number. The possible values are:

    * `ClickableLink` - send the user a code with a clickable link.
    * `FormField` - send the user a short code intended to be manually entered into a form field. This is only available when <InlineField>{props.base_field_name}.phoneConfiguration.unverified.behavior</InlineField> has the `Gated` value.
  </APIField>

  <APIField name={props.base_field_name + '.phoneConfiguration.verificationTemplateId'} type="UUID" since="1.59.0">
    The Id of the Message Template used to send SMS messages to users to verify that their phone number is valid. {props.application_phone_config_override_text}
  </APIField>

  <APIField name={props.base_field_name + '.phoneConfiguration.verifyPhoneNumber'} type="Boolean" since="1.59.0" renderif={props.is_tenant}>
    Whether a user's phone number is verified when they register with your application.
  </APIField>
