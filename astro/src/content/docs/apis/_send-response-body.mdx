import APIBlock from 'src/components/api/APIBlock.astro';
import APIField from 'src/components/api/APIField.astro';
import InlineField from 'src/components/InlineField.astro';
import JSON from 'src/components/JSON.astro';

#### Response Body

<APIBlock>
  <APIField name="anonymousResults" type="Map<String,Object>" since="1.28.0">
    This is a Mapping between the email address that had errors and the errors that were encountered. This mapping is used for errors that occur when using the <InlineField>toAddresses</InlineField> field on the send request.
  </APIField>
  <APIField name="anonymousResults[email].parseErrors" type="Map<String,String>" since="1.28.0">
    Contains any parse errors that occurred while parsing the Email Template parts. The key of the Map is the field that failed (i.e. **from**) and the value is the error message.
  </APIField>
  <APIField name="anonymousResults[email].renderErrors" type="Map<String,String>" since="1.28.0">
    Contains any template errors that occurred while executing the Email Template parts. The key of the Map is the field that failed (i.e. **from**) and the value is the error message.
  </APIField>
  <APIField name="results" type="Map<UUID,Object>">
    This is a Mapping between the User Ids that had errors and the errors that were encountered.
  </APIField>
  <APIField name="results[userId].parseErrors" type="Map<String,String>">
    Contains any parse errors that occurred while parsing the Email Template parts. The key of the Map is the field that failed (i.e. **from**) and the value is the error message.
  </APIField>
  <APIField name="results[userId].renderErrors" type="Map<String,String>">
    Contains any template errors that occurred while executing the Email Template parts. The key of the Map is the field that failed (i.e. **from**) and the value is the error message.
  </APIField>
</APIBlock>

<JSON title="Example Response JSON" src="email-templates/send-response.json" />
