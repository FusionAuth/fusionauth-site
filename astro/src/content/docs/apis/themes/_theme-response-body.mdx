import APIBlock from 'src/components/api/APIBlock.astro';
import APIField from 'src/components/api/APIField.astro';
import InlineField from 'src/components/InlineField.astro';

#### Response Body

<APIBlock>
  <APIField name="theme.data" type="Object">
    An object that can hold any information about the Theme that should be persisted.
  </APIField>
  <APIField name="theme.defaultMessages" type="String">
    A properties file formatted String containing messages used within the templates.
  </APIField>
  <APIField name="theme.id" type="UUID">
    The unique Id of the Theme.
  </APIField>
  <APIField name="theme.insertInstant" type="Long">
    The [instant](/docs/reference/data-types#instants) that the theme was added to the FusionAuth database.
  </APIField>
  <APIField name="theme.lastUpdateInstant" type="Long">
    The [instant](/docs/reference/data-types#instants) that the theme was last updated in the FusionAuth database.
  </APIField>
  <APIField name="theme.localizedMessages" type="Map<Locale,String>">
    A Map of localized versions of the messages. The key is the Locale and the value is a properties file formatted String.
  </APIField>
  <APIField name="theme.name" type="String">
    A unique name for the Theme.
  </APIField>
  <APIField name="theme.stylesheet" type="String" renderif={!props.simple}>
    A CSS stylesheet used to style the templates.
  </APIField>
</APIBlock>

