import APIField from 'src/components/api/APIField.astro';
import InlineField from 'src/components/InlineField.astro';

<APIField name={(props.search ? "themes[x]." : "theme.") + "variables.alertBackgroundColor"} type="String" >
  The background color of the alert message. 
</APIField>

<APIField name={(props.search ? "themes[x]." : "theme.") + "variables.alertFontColor"} type="String" >
  The color of the font in the alert message. 
</APIField>

<APIField name={(props.search ? "themes[x]." : "theme.") + "variables.backgroundImageURL"} type="String" optional>
  The url of the background image. Will replace the page background color. 
</APIField>

<APIField name={(props.search ? "themes[x]." : "theme.") + "variables.backgroundSize"} type="String" optional>
  Specifies the [background-size](https://developer.mozilla.org/en-US/docs/Web/CSS/background-size) of the background image. 
</APIField>

<APIField name={(props.search ? "themes[x]." : "theme.") + "variables.borderRadius"} type="String" >
  Specifies the [border-radius](https://developer.mozilla.org/en-US/docs/Web/CSS/border-radius) property which is applied to buttons, panels, and form fields in the theme. 
</APIField>

<APIField name={(props.search ? "themes[x]." : "theme.") + "variables.deleteButtonColor"} type="String" >
  The color of the delete button. 
</APIField>

<APIField name={(props.search ? "themes[x]." : "theme.") + "variables.deleteButtonFocusColor"} type="String" >
  The color of the delete button when clicked. 
</APIField>

<APIField name={(props.search ? "themes[x]." : "theme.") + "variables.deleteButtonTextColor"} type="String" >
  The color of the text in the delete button. 
</APIField>

<APIField name={(props.search ? "themes[x]." : "theme.") + "variables.deleteButtonTextFocusColor"} type="String" >
  The color of the text in the delete button when clicked. 
</APIField>

<APIField name={(props.search ? "themes[x]." : "theme.") + "variables.errorIconColor"} type="String" >
  The color of the icon in error alert messages in the theme. 
</APIField>

<APIField name={(props.search ? "themes[x]." : "theme.") + "variables.errorFontColor"} type="String" >
  The color of the font for error messages. 
</APIField>

<APIField name={(props.search ? "themes[x]." : "theme.") + "variables.fontColor"} type="String" >
  The color of the text in the theme. 
</APIField>

<APIField name={(props.search ? "themes[x]." : "theme.") + "variables.fontFamily"} type="String" >
  Specifies the [font-family](https://developer.mozilla.org/en-US/docs/Web/CSS/font-family) for the theme. 
</APIField>

<APIField name={(props.search ? "themes[x]." : "theme.") + "variables.footerDisplay"} type="Boolean" optional>
  Determines if the "powered by FusionAuth" footer is displayed in the theme. 
</APIField>

<APIField name={(props.search ? "themes[x]." : "theme.") + "variables.iconBackgroundColor"} type="String" >
  The color of the form field icon background. 
</APIField>

<APIField name={(props.search ? "themes[x]." : "theme.") + "variables.iconColor"} type="String" >
  The color of the icons in the theme. 
</APIField>

<APIField name={(props.search ? "themes[x]." : "theme.") + "variables.infoIconColor"} type="String" >
  The color of the icon in info alert messages in the theme. 
</APIField>

<APIField name={(props.search ? "themes[x]." : "theme.") + "variables.inputBackgroundColor"} type="String" >
  The color of the form field background. 
</APIField>

<APIField name={(props.search ? "themes[x]." : "theme.") + "variables.inputIconColor"} type="String" >
  The color of the form field icon. 
</APIField>

<APIField name={(props.search ? "themes[x]." : "theme.") + "variables.inputTextColor"} type="String" >
  The color of the form field text. 
</APIField>

<APIField name={(props.search ? "themes[x]." : "theme.") + "variables.linkTextColor"} type="String" >
  The color of the text in hyperlinks. 
</APIField>

<APIField name={(props.search ? "themes[x]." : "theme.") + "variables.linkTextFocusColor"} type="String" >
  The color of the text in hyperlinks that have been visited. 
</APIField>

<APIField name={(props.search ? "themes[x]." : "theme.") + "variables.logoImageSize"} type="String" optional>
  The size of the logo image. 
</APIField>

<APIField name={(props.search ? "themes[x]." : "theme.") + "variables.logoImageURL"} type="String" optional>
  The url of the logo image. 
</APIField>

<APIField name={(props.search ? "themes[x]." : "theme.") + "variables.monoFontColor"} type="String" >
  The color of the monospaced text. 
</APIField>

<APIField name={(props.search ? "themes[x]." : "theme.") + "variables.monoFontFamily"} type="String" >
  The font [font-family](https://developer.mozilla.org/en-US/docs/Web/CSS/font-family). 
</APIField>

<APIField name={(props.search ? "themes[x]." : "theme.") + "variables.pageBackgroundColor"} type="String" >
  The color of the page background. 
</APIField>

<APIField name={(props.search ? "themes[x]." : "theme.") + "variables.panelBackgroundColor"} type="String" >
  The color of the panels in the theme. 
</APIField>

<APIField name={(props.search ? "themes[x]." : "theme.") + "variables.primaryButtonColor"} type="String" >
  The color of the primary button. 
</APIField>

<APIField name={(props.search ? "themes[x]." : "theme.") + "variables.primaryButtonFocusColor"} type="String" >
  The color of primary button when clicked. 
</APIField>

<APIField name={(props.search ? "themes[x]." : "theme.") + "variables.primaryButtonTextColor"} type="String" >
  The color of the text in the primary button. 
</APIField>

<APIField name={(props.search ? "themes[x]." : "theme.") + "variables.primaryButtonTextFocusColor"} type="String" >
  The color of the text in the primary button when clicked. 
</APIField>
