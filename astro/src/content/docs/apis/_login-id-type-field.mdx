import APIField from 'src/components/api/APIField.astro';
import InlineField from 'src/components/InlineField.astro';
import Aside from 'src/components/Aside.astro';

<APIField name={props.plural ? 'loginIdTypes' : 'loginIdType'} type={props.plural ? 'Array<String>' : 'String'} defaults={props.plural ? '[email, username]' : 'email or username if email not found'} optional since="1.99.9">
  The identity type{props.plural ? 's' : ''} that FusionAuth will compare the <InlineField>loginId</InlineField> to. Can be one{props.plural ? ' or more' : ''} of the following:

  * `email`
  * `phoneNumber`
  * `username`

  { props.plural &&
    <Aside>
      The order is significant. If <code>["email", "username"]</code> is supplied with a <InlineField>loginId</InlineField> value of <code>terry@example.com</code>, then a user with <code>terry@example.com</code> as their email address will match first before any user with <code>terry@example.com</code> as their username.
    </Aside>
  }
</APIField>
