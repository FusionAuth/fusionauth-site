import APIField from 'src/components/api/APIField.astro';
import InlineField from 'src/components/InlineField.astro';
import Aside from 'src/components/Aside.astro';

<APIField name="loginIdType" type="String" defaults="email, username if email not found" optional since="1.99.9">
  Type that FusionAuth will attempt to resolve a user identity with, using <InlineField>user.identities\[x].type</InlineField> and <InlineField>loginId</InlineField>. Can be one of the following:

  * `email`
  * `phoneNumber`
  * `username`
</APIField>
