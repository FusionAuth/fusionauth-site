import InlineField from 'src/components/InlineField.astro';
import APIField from 'src/components/api/APIField.astro';

{props.idp_type !== "Nintendo" && <>

<APIField name="identityProvider.tenantConfiguration" type="Map<UUID, Object>" optional={props.optional_tag} since="1.32.0">
  The configuration for each Tenant that limits the number of links a user may have for a particular identity provider.
</APIField>
<APIField name="identityProvider.tenantConfiguration[tenantId].limitUserLinkCount" type="Object" optional={props.optional_tag} since="1.32.0">
  The policy that governs identity provider links on a Tenant.
</APIField>
<APIField name="identityProvider.tenantConfiguration[tenantId].limitUserLinkCount.enabled" type="Boolean" defaults="false" optional={props.optional_tag} since="1.32.0">
  When enabled, the number of identity provider links a user may create is enforced by <InlineField>maximumLinks</InlineField>.
</APIField>
<APIField name="identityProvider.tenantConfiguration[tenantId].limitUserLinkCount.maximumLinks" type="Integer" defaults="42" optional={props.optional_tag} since="1.32.0">
  The maximum number of links that a user can have for a particular identity provider. This configuration is specific to this IdP. So a user can have more links than this number, but for this particular IdP, the number of links will be limited and enforced.
</APIField>

</>}

{props.idp_type === "Nintendo" && <>

<APIField name="identityProvider.tenantConfiguration" type="Map<UUID, Object>" optional={props.optional_tag}>
  The configuration for each Tenant that limits the number of links a user may have for a particular identity provider.
</APIField>
<APIField name="identityProvider.tenantConfiguration[tenantId].limitUserLinkCount" type="Object" optional={props.optional_tag}>
  The policy that governs identity provider links on a Tenant.
</APIField>
<APIField name="identityProvider.tenantConfiguration[tenantId].limitUserLinkCount.enabled" type="Boolean" defaults="false" optional={props.optional_tag}>
  When enabled, the number of identity provider links a user may create is enforced by <InlineField>maximumLinks</InlineField>.
</APIField>
<APIField name="identityProvider.tenantConfiguration[tenantId].limitUserLinkCount.maximumLinks" type="Integer" defaults="42" optional={props.optional_tag}>
  The maximum number of links that a user can have for a particular identity provider. This configuration is specific to this IdP. So a user can have more links than this number, but for this particular IdP, the number of links will be limited and enforced.
</APIField>

</>}
