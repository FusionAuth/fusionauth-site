---
import JSON from 'src/components/JSON.astro';

const {show_code_and_token_examples, idp_request} = Astro.props;

---

{ show_code_and_token_examples && <JSON title="Example Request JSON using code and redirect_uri" src={"identity-providers/" + idp_request + "-code-request.json"} /> }

{ show_code_and_token_examples && <JSON title="Example Request JSON using token" src={"identity-providers/" + idp_request + "-token-request.json"} /> }

{ !show_code_and_token_examples && <JSON title="Example Request JSON" src={"identity-providers/" + idp_request + "-request.json"} /> }
