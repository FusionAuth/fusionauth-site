import APIField from 'src/components/api/APIField.astro';

<APIField name="identityProvider.source" type="String" optional defaults="System">
  The source of this Identity Provider.

  The maximum length is `191` characters.

  Identity Providers created by Tenant Manager use `Tenant Manager`, which is required for Tenant Manager SSO visibility

  This value is only used on create. If provided on update, it is ignored and the existing value is preserved.
</APIField>
