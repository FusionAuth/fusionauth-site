import APIField from 'src/components/api/APIField.astro';

<APIField name="identityProvider.lambdaConfiguration.reconcileId" type="UUID" optional since={props.idp_since < 11700 ? "1.17.0" : ''}>
  The unique Id of the Lambda used during the user reconcile process to map custom claims from the external identity provider to the FusionAuth user.

  The specified Lambda Id must be of type <code>{props.idp_type} Reconcile</code>.

  This property cannot be used with <code>identityProvider.attributeMappings</code>.
</APIField>
