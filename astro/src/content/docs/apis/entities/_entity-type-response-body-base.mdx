import APIBlock from 'src/components/api/APIBlock.astro';
import APIField from 'src/components/api/APIField.astro';
import InlineField from 'src/components/InlineField.astro';
import JSON from 'src/components/JSON.astro';

<APIBlock>
  <APIField name={props.base_field_name} type="Array" renderif={props.base_field_name !== 'entityType'}>
    The list of EntityType objects.
  </APIField>
  <APIField name={props.base_field_name + ".data"} type="Object">
    An object that can hold any information about the Entity Type that should be persisted.
  </APIField>
  <APIField name={props.base_field_name + ".id"} type="UUID">
    The Entity Type's unique Id.
  </APIField>
  <APIField name={props.base_field_name + ".insertInstant"} type="Long">
    The [instant](/docs/reference/data-types#instants) that the Entity Type was added to the FusionAuth database.
  </APIField>
  <APIField name={props.base_field_name + ".jwtConfiguration.accessTokenKeyId"} type="UUID">
    The unique id of the signing key used to sign the access token.
  </APIField>
  <APIField name={props.base_field_name + ".jwtConfiguration.enabled"} type="Boolean">
    Indicates if this application is using the JWT configuration defined here.
  </APIField>
  <APIField name={props.base_field_name + ".jwtConfiguration.timeToLiveInSeconds"} type="Integer">
    The length of time in seconds the JWT will live before it is expired and no longer valid.
  </APIField>
  <APIField name={props.base_field_name + ".lastUpdateInstant"} type="Long">
    The [instant](/docs/reference/data-types#instants) that the Entity Type was last updated in the FusionAuth database.
  </APIField>
  <APIField name={props.base_field_name + ".name"} type="Long">
    The name of the entity type (i.e. "Customer" or "Email_Service").
  </APIField>
  <APIField name={props.base_field_name + ".permissions"} type="Array">
    An array of Permission objects.
  </APIField>
  <APIField name={props.base_field_name + ".permissions[x].data"} type="Object">
    An object that can hold any information about the Permission that should be persisted.
  </APIField>
  <APIField name={props.base_field_name + ".permissions[x].description"} type="String">
    A description of the Permission.
  </APIField>
  <APIField name={props.base_field_name + ".permissions[x].id"} type="UUID">
    The Id of the Permission.
  </APIField>
  <APIField name={props.base_field_name + ".permissions[x].insertInstant"} type="Long">
    The [instant](/docs/reference/data-types#instants) that the Permission was added to the FusionAuth database.
  </APIField>
  <APIField name={props.base_field_name + ".permissions[x].isDefault"} type="Boolean">
    Whether or not the Permission is default. A default Permission is automatically assigned to an Entity if no permissions are provided.
  </APIField>
  <APIField name={props.base_field_name + ".permissions[x].lastUpdateInstant"} type="Long">
    The [instant](/docs/reference/data-types#instants) that the Permission was last updated in the FusionAuth database.
  </APIField>
  <APIField name={props.base_field_name + ".permissions[x].name"} type="String">
    The name of the Permission.
  </APIField>
</APIBlock>

{props.base_field_name === 'entityType' && <JSON title="Example Response JSON for a Single Entity Type" src="entity-types/response.json" /> }

{props.base_field_name !== 'entityType' && <JSON title="Example Response JSON for all Entity Types" src="entity-types/responses.json" /> }
