import APIBlock from 'src/components/api/APIBlock.astro';
import APIField from 'src/components/api/APIField.astro';
import DeviceTypeList from 'src/content/docs/_shared/_device-type-list.mdx';
import InlineField from 'src/components/InlineField.astro';
import JSON from 'src/components/JSON.astro';

<APIBlock>
  <APIField name="refreshTokens" type="Array" renderif={props.base_field_name !== 'refreshToken'}>
    An array of Refresh Tokens
  </APIField>
  <APIField name={ props.base_field_name + ".applicationId" } type="String">
    The Id of the Application for which this token is authorized to be exchanged for a new Access Token. If this Id is not present, the token cannot be used to obtain an access token for an Application.
  </APIField>
  <APIField name={ props.base_field_name + ".insertInstant" } type="Long">
    The [instant](/docs/reference/data-types#instants) this Refresh Token was issued.
  </APIField>
  <APIField name={ props.base_field_name + ".id" } type="UUID">
    The Id of the Refresh Token.
  </APIField>
  <APIField name={ props.base_field_name + ".metaData.device.description" } type="String">
    A description of the device. For example, `Hooli Phone 8GB Work Phone`.
  </APIField>
  <APIField name={ props.base_field_name + ".metaData.device.lastAccessedAddress" } type="String">
    The IP address of the device when this Refresh Token was last used.
  </APIField>
  <APIField name={ props.base_field_name + ".metaData.device.lastAccessedInstant" } type="Long">
    The [instant](/docs/reference/data-types#instants) this Refresh Token was last used.
  </APIField>
  <APIField name={ props.base_field_name + ".metaData.device.name" } type="String">
    The name of the device, for example `Richard's Hooli Phone`.
  </APIField>
  <APIField name={ props.base_field_name + ".metaData.device.type" } type="String">
    The type of device represented by the `device` parameter.

    Prior to version 1.46.0, this value was restricted to the following types:

<DeviceTypeList />

    In version `1.46.0` and beyond, this value can be any string value you'd like, have fun with it!
  </APIField>
  <APIField name={ props.base_field_name + ".metaData.resources" } type="Array<String>" since="1.67.0">
    The resource URIs associated with this Refresh Token, per [RFC 8707 (Resource Indicators for OAuth 2.0)](https://www.rfc-editor.org/rfc/rfc8707.html). This field is populated during the authorization code exchange when a `resource` parameter was provided and validated against the application's <InlineField>oauthConfiguration.authorizedResourceUris</InlineField>.

    These values are used as the reference point for resource validation in subsequent refresh token grants and `/api/jwt/refresh` calls.
  </APIField>
  <APIField name={ props.base_field_name + ".metaData.scopes" } type="Array<String>">
    The scopes associated with this Refresh Token. These are set at authentication when the Refresh Token is first created.
  </APIField>
  <APIField name={ props.base_field_name + ".startInstant" } type="Long">
    The [instant](/docs/reference/data-types#instants) of the start of this Refresh Token. This value will be used to calculate token expiration.
  </APIField>
  <APIField name={ props.base_field_name + ".token" } type="String">
    The string representation of the encoded Refresh Token. This value should be kept in some sort of secure storage and treated as sensitive information.
  </APIField>
  <APIField name={ props.base_field_name + ".userId" } type="UUID">
    The User Id of the user for which this Refresh Token was issued.
  </APIField>
</APIBlock>
 
{props.base_field_name  === "refreshToken" && <JSON title="Example Response JSON For a Single Refresh Token" src="jwt/refresh-token-get-response.json" /> }

{props.base_field_name  !== "refreshToken" &&  <JSON title="Example Response JSON For All Refresh Tokens For a User" src="jwt/refresh-tokens-get-response.json" /> }
