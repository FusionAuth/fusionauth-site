import APIBlock from 'src/components/api/APIBlock.astro';
import APIField from 'src/components/api/APIField.astro';
import InlineFieldValue from 'src/components/InlineFieldValue.astro';
import JSON from 'src/components/JSON.astro';

<APIBlock>
  <APIField name={ props.base_field_name + "" } type="Array" renderif={props.base_field_name !== "messageTemplate"}>
    The list of Message Template objects.
  </APIField>
  <APIField name={ props.base_field_name + ".data" } type="Object">
    An object that can hold any information about the Message Template that should be persisted.
  </APIField>
  <APIField name={ props.base_field_name + ".defaultTemplate" } type="[String (FreeMarker Enabled)]">
    The default Message Template.
  </APIField>
  <APIField name={ props.base_field_name + ".id" } type="UUID">
    The unique identifier for this Message Template.
  </APIField>
  <APIField name={ props.base_field_name + ".insertInstant" } type="Long">
    The [instant](/docs/reference/data-types#instants) that the Application was added to the FusionAuth database.
  </APIField>
  <APIField name={ props.base_field_name + ".lastUpdateInstant" } type="Long">
    The [instant](/docs/reference/data-types#instants) that the Application was last updated in the FusionAuth database.
  </APIField>
  <APIField name={ props.base_field_name + ".localizedTemplates" } type="Map<Locale,String (FreeMarker Enabled)>">
    The Message Template used when sending messages to users who speak other languages. This overrides the default Message Template based on the user's list of preferred languages.
  </APIField>
  <APIField name={ props.base_field_name + ".name" } type="String">
    A descriptive name for the message template.
  </APIField>
  <APIField name={ props.base_field_name + ".type" } type="String">
    The type of the template. This will have the value <InlineFieldValue>SMS</InlineFieldValue>.
  </APIField>
</APIBlock>

{props.base_field_name === "messageTemplate" && <JSON title="Example Response JSON for a Single Message Template" src="message-templates/response.json" /> }

{props.base_field_name !== "messageTemplate" && <JSON title="Example Response JSON for all Message Templates" src="message-templates/responses.json" /> }
