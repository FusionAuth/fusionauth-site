import APIField from 'src/components/api/APIField.astro';
import InlineField from 'src/components/InlineField.astro';

<APIField name={(props.search ? "themes[x]." : "theme.") + "variables.alertBackgroundColor"} type="String" since="1.51.0" optional>
  The background color of the alert message. {props.request && <span><a href="https://developer.mozilla.org/en-US/docs/Web/CSS/color">Colors</a> must be a valid hex color code, RGB, or HSL value. Required if <InlineField>theme.type</InlineField> is <code>simple</code>.</span> }

  {!props.request && <p>Only present if <InlineField>theme.type</InlineField> is <code>simple</code>.</p> }
</APIField>

<APIField name={(props.search ? "themes[x]." : "theme.") + "variables.alertFontColor"} type="String" since="1.51.0" optional>
  The color of the font in the alert message. {props.request && <span><a href="https://developer.mozilla.org/en-US/docs/Web/CSS/color">Colors</a> must be a valid hex color code, RGB, or HSL value. Required if <InlineField>theme.type</InlineField> is <code>simple</code>.</span> }

  {!props.request && <p>Only present if <InlineField>theme.type</InlineField> is <code>simple</code>.</p> }
</APIField>

<APIField name={(props.search ? "themes[x]." : "theme.") + "variables.backgroundImageUrl"} type="String" since="1.51.0" optional>
  The url of the background image. Will replace the page background color. {props.request && <span>Must be a valid absolute URL. Can be <code>null</code>.</span> }

  {!props.request && <p>Only present if <InlineField>theme.type</InlineField> is <code>simple</code>.</p> }
</APIField>

<APIField name={(props.search ? "themes[x]." : "theme.") + "variables.backgroundSize"} type="String" since="1.51.0" optional>
  Specifies the [background-size](https://developer.mozilla.org/en-US/docs/Web/CSS/background-size) of the background image. {props.request && <span>Valid values are <code>repeat</code>, <code>contain</code>, and <code>cover</code>.Required if <InlineField>theme.type</InlineField> is <code>simple</code> and <InlineField>theme.variables.backgroundImageUrl</InlineField> is not provided.</span>}

  {!props.request && <p>Only present if <InlineField>theme.type</InlineField> is <code>simple</code>.</p> }
</APIField>

<APIField name={(props.search ? "themes[x]." : "theme.") + "variables.borderRadius"} type="String" since="1.51.0" optional>
  Specifies the [border-radius](https://developer.mozilla.org/en-US/docs/Web/CSS/border-radius) property which is applied to buttons, panels, and form fields in the theme. {props.request && <span>Must be a valid numeric value with a <code>px</code>, <code>em</code>, or <code>rem</code> suffix. Required if <InlineField>theme.type</InlineField> is <code>simple</code>.</span> }

  {!props.request && <p>Only present if <InlineField>theme.type</InlineField> is <code>simple</code>.</p> }
</APIField>

<APIField name={(props.search ? "themes[x]." : "theme.") + "variables.deleteButtonColor"} type="String" since="1.51.0" optional>
  The color of the delete button. {props.request && <span><a href="https://developer.mozilla.org/en-US/docs/Web/CSS/color">Colors</a> must be a valid hex color code, RGB, or HSL value. Required if <InlineField>theme.type</InlineField> is <code>simple</code>.</span> }

  {!props.request && <p>Only present if <InlineField>theme.type</InlineField> is <code>simple</code>.</p> }
</APIField>

<APIField name={(props.search ? "themes[x]." : "theme.") + "variables.deleteButtonFocusColor"} type="String" since="1.51.0" optional>
  The color of the delete button when clicked. {props.request && <span><a href="https://developer.mozilla.org/en-US/docs/Web/CSS/color">Colors</a> must be a valid hex color code, RGB, or HSL value. Required if <InlineField>theme.type</InlineField> is <code>simple</code>.</span> }

  {!props.request && <p>Only present if <InlineField>theme.type</InlineField> is <code>simple</code>.</p> }
</APIField>

<APIField name={(props.search ? "themes[x]." : "theme.") + "variables.deleteButtonTextColor"} type="String" since="1.51.0" optional>
  The color of the text in the delete button. {props.request && <span><a href="https://developer.mozilla.org/en-US/docs/Web/CSS/color">Colors</a> must be a valid hex color code, RGB, or HSL value. Required if <InlineField>theme.type</InlineField> is <code>simple</code>.</span> }

  {!props.request && <p>Only present if <InlineField>theme.type</InlineField> is <code>simple</code>.</p> }
</APIField>

<APIField name={(props.search ? "themes[x]." : "theme.") + "variables.deleteButtonTextFocusColor"} type="String" since="1.51.0" optional>
  The color of the text in the delete button when clicked. {props.request && <span><a href="https://developer.mozilla.org/en-US/docs/Web/CSS/color">Colors</a> must be a valid hex color code, RGB, or HSL value. Required if <InlineField>theme.type</InlineField> is <code>simple</code>.</span> }

  {!props.request && <p>Only present if <InlineField>theme.type</InlineField> is <code>simple</code>.</p> }
</APIField>

<APIField name={(props.search ? "themes[x]." : "theme.") + "variables.errorIconColor"} type="String" since="1.51.0" optional>
  The color of the icon in error alert messages in the theme. {props.request && <span><a href="https://developer.mozilla.org/en-US/docs/Web/CSS/color">Colors</a> must be a valid hex color code, RGB, or HSL value. Required if <InlineField>theme.type</InlineField> is <code>simple</code>.</span> }

  {!props.request && <p>Only present if <InlineField>theme.type</InlineField> is <code>simple</code>.</p> }
</APIField>

<APIField name={(props.search ? "themes[x]." : "theme.") + "variables.errorFontColor"} type="String" since="1.51.0" optional>
  The color of the font for error messages. {props.request && <span><a href="https://developer.mozilla.org/en-US/docs/Web/CSS/color">Colors</a> must be a valid hex color code, RGB, or HSL value. Required if <InlineField>theme.type</InlineField> is <code>simple</code>.</span> }

  {!props.request && <p>Only present if <InlineField>theme.type</InlineField> is <code>simple</code>.</p> }
</APIField>

<APIField name={(props.search ? "themes[x]." : "theme.") + "variables.fontColor"} type="String" since="1.51.0" optional>
  The color of the text in the theme. {props.request && <span><a href="https://developer.mozilla.org/en-US/docs/Web/CSS/color">Colors</a> must be a valid hex color code, RGB, or HSL value. Required if <InlineField>theme.type</InlineField> is <code>simple</code>.</span> }

  {!props.request && <p>Only present if <InlineField>theme.type</InlineField> is <code>simple</code>.</p> }
</APIField>

<APIField name={(props.search ? "themes[x]." : "theme.") + "variables.fontFamily"} type="String" since="1.51.0" optional>
  Specifies the [font-family](https://developer.mozilla.org/en-US/docs/Web/CSS/font-family) for the theme. {props.request && <span>Required if <InlineField>theme.type</InlineField> is <code>simple</code>.</span> }

  {!props.request && <p>Only present if <InlineField>theme.type</InlineField> is <code>simple</code>.</p> }
</APIField>

<APIField name={(props.search ? "themes[x]." : "theme.") + "variables.footerDisplay"} type="Booleans" since="1.51.0" optional>
  Determines if the "powered by FusionAuth" footer is displayed in the theme. {props.request && <span>A valid license is required to set this value to <code>false</code>. Required if <InlineField>theme.type</InlineField> is <code>simple</code>.</span> }

  {!props.request && <p>Only present if <InlineField>theme.type</InlineField> is <code>simple</code>.</p> }
</APIField>

<APIField name={(props.search ? "themes[x]." : "theme.") + "variables.iconBackgroundColor"} type="String" since="1.51.0" optional>
  The color of the form field icon background. {props.request && <span><a href="https://developer.mozilla.org/en-US/docs/Web/CSS/color">Colors</a> must be a valid hex color code, RGB, or HSL value. Required if <InlineField>theme.type</InlineField> is <code>simple</code>.</span> }

  {!props.request && <p>Only present if <InlineField>theme.type</InlineField> is <code>simple</code>.</p> }
</APIField>

<APIField name={(props.search ? "themes[x]." : "theme.") + "variables.iconColor"} type="String" since="1.51.0" optional>
  The color of the icons in the theme. {props.request && <span><a href="https://developer.mozilla.org/en-US/docs/Web/CSS/color">Colors</a> must be a valid hex color code, RGB, or HSL value. Required if <InlineField>theme.type</InlineField> is <code>simple</code>.</span> }

  {!props.request && <p>Only present if <InlineField>theme.type</InlineField> is <code>simple</code>.</p> }
</APIField>

<APIField name={(props.search ? "themes[x]." : "theme.") + "variables.infoIconColor"} type="String" since="1.51.0" optional>
  The color of the icon in info alert messages in the theme. {props.request && <span><a href="https://developer.mozilla.org/en-US/docs/Web/CSS/color">Colors</a> must be a valid hex color code, RGB, or HSL value. Required if <InlineField>theme.type</InlineField> is <code>simple</code>.</span> }

  {!props.request && <p>Only present if <InlineField>theme.type</InlineField> is <code>simple</code>.</p> }
</APIField>

<APIField name={(props.search ? "themes[x]." : "theme.") + "variables.inputBackgroundColor"} type="String" since="1.51.0" optional>
  The color of the form field background. {props.request && <span><a href="https://developer.mozilla.org/en-US/docs/Web/CSS/color">Colors</a> must be a valid hex color code, RGB, or HSL value. Required if <InlineField>theme.type</InlineField> is <code>simple</code>.</span> }

  {!props.request && <p>Only present if <InlineField>theme.type</InlineField> is <code>simple</code>.</p> }
</APIField>

<APIField name={(props.search ? "themes[x]." : "theme.") + "variables.inputIconColor"} type="String" since="1.51.0" optional>
  The color of the form field icon. {props.request && <span><a href="https://developer.mozilla.org/en-US/docs/Web/CSS/color">Colors</a> must be a valid hex color code, RGB, or HSL value. Required if <InlineField>theme.type</InlineField> is <code>simple</code>.</span> }

  {!props.request && <p>Only present if <InlineField>theme.type</InlineField> is <code>simple</code>.</p> }
</APIField>

<APIField name={(props.search ? "themes[x]." : "theme.") + "variables.inputTextColor"} type="String" since="1.51.0" optional>
  The color of the form field text. {props.request && <span><a href="https://developer.mozilla.org/en-US/docs/Web/CSS/color">Colors</a> must be a valid hex color code, RGB, or HSL value. Required if <InlineField>theme.type</InlineField> is <code>simple</code>.</span> }

  {!props.request && <p>Only present if <InlineField>theme.type</InlineField> is <code>simple</code>.</p> }
</APIField>

<APIField name={(props.search ? "themes[x]." : "theme.") + "variables.linkTextColor"} type="String" since="1.51.0" optional>
  The color of the text in hyperlinks. {props.request && <span><a href="https://developer.mozilla.org/en-US/docs/Web/CSS/color">Colors</a> must be a valid hex color code, RGB, or HSL value. Required if <InlineField>theme.type</InlineField> is <code>simple</code>.</span> }

  {!props.request && <p>Only present if <InlineField>theme.type</InlineField> is <code>simple</code>.</p> }
</APIField>

<APIField name={(props.search ? "themes[x]." : "theme.") + "variables.linkTextFocusColor"} type="String" since="1.51.0" optional>
  The color of the text in hyperlinks that have been visited. {props.request && <span><a href="https://developer.mozilla.org/en-US/docs/Web/CSS/color">Colors</a> must be a valid hex color code, RGB, or HSL value. Required if <InlineField>theme.type</InlineField> is <code>simple</code>.</span> }

  {!props.request && <p>Only present if <InlineField>theme.type</InlineField> is <code>simple</code>.</p> }
</APIField>

<APIField name={(props.search ? "themes[x]." : "theme.") + "variables.logoImageSize"} type="String" since="1.51.0" optional>
  The size of the logo image. {props.request && <span>Must be a valid numeric value with a <code>px</code>, <code>em</code>, or <code>rem</code> suffix. Required if <InlineField>theme.type</InlineField> is <code>simple</code> and <InlineField>theme.variables.logoImageUrl</InlineField> is provided.</span> }

  {!props.request && <p>Only present if <InlineField>theme.type</InlineField> is <code>simple</code>.</p> }
</APIField>

<APIField name={(props.search ? "themes[x]." : "theme.") + "variables.logoImageUrl"} type="String" since="1.51.0" optional>
  The url of the logo image. {props.request && <span>Must be a valid absolute URL. Can be <code>null</code>.</span> }

  {!props.request && <p>Only present if <InlineField>theme.type</InlineField> is <code>simple</code>.</p> }
</APIField>

<APIField name={(props.search ? "themes[x]." : "theme.") + "variables.monoFontColor"} type="String" since="1.51.0" optional>
  The color of the monospaced text. {props.request && <span><a href="https://developer.mozilla.org/en-US/docs/Web/CSS/color">Colors</a> must be a valid hex color code, RGB, or HSL value. Required if <InlineField>theme.type</InlineField> is <code>simple</code>.</span> }

  {!props.request && <p>Only present if <InlineField>theme.type</InlineField> is <code>simple</code>.</p> }
</APIField>

<APIField name={(props.search ? "themes[x]." : "theme.") + "variables.monoFontFamily"} type="String" since="1.51.0" optional>
  The font [font-family](https://developer.mozilla.org/en-US/docs/Web/CSS/font-family). {props.request && <span>Required if <InlineField>theme.type</InlineField> is <code>simple</code></span> }

  {!props.request && <p>Only present if <InlineField>theme.type</InlineField> is <code>simple</code>.</p> }
</APIField>

<APIField name={(props.search ? "themes[x]." : "theme.") + "variables.pageBackgroundColor"} type="String" since="1.51.0" optional>
  The color of the page background. {props.request && <span><a href="https://developer.mozilla.org/en-US/docs/Web/CSS/color">Colors</a> must be a valid hex color code, RGB, or HSL value. Required if <InlineField>theme.type</InlineField> is <code>simple</code>.</span> }

  {!props.request && <p>Only present if <InlineField>theme.type</InlineField> is <code>simple</code>.</p> }
</APIField>

<APIField name={(props.search ? "themes[x]." : "theme.") + "variables.panelBackgroundColor"} type="String" since="1.51.0" optional>
  The color of the panels in the theme. {props.request && <span><a href="https://developer.mozilla.org/en-US/docs/Web/CSS/color">Colors</a> must be a valid hex color code, RGB, or HSL value. Required if <InlineField>theme.type</InlineField> is <code>simple</code>.</span> }

  {!props.request && <p>Only present if <InlineField>theme.type</InlineField> is <code>simple</code>.</p> }
</APIField>

<APIField name={(props.search ? "themes[x]." : "theme.") + "variables.primaryButtonColor"} type="String" since="1.51.0" optional>
  The color of the primary button. {props.request && <span><a href="https://developer.mozilla.org/en-US/docs/Web/CSS/color">Colors</a> must be a valid hex color code, RGB, or HSL value. Required if <InlineField>theme.type</InlineField> is <code>simple</code>.</span> }

  {!props.request && <p>Only present if <InlineField>theme.type</InlineField> is <code>simple</code>.</p> }
</APIField>

<APIField name={(props.search ? "themes[x]." : "theme.") + "variables.primaryButtonFocusColor"} type="String" since="1.51.0" optional>
  The color of primary button when clicked. {props.request && <span><a href="https://developer.mozilla.org/en-US/docs/Web/CSS/color">Colors</a> must be a valid hex color code, RGB, or HSL value. Required if <InlineField>theme.type</InlineField> is <code>simple</code>.</span> }

  {!props.request && <p>Only present if <InlineField>theme.type</InlineField> is <code>simple</code>.</p> }
</APIField>

<APIField name={(props.search ? "themes[x]." : "theme.") + "variables.primaryButtonTextColor"} type="String" since="1.51.0" optional>
  The color of the text in the primary button. {props.request && <span><a href="https://developer.mozilla.org/en-US/docs/Web/CSS/color">Colors</a> must be a valid hex color code, RGB, or HSL value. Required if <InlineField>theme.type</InlineField> is <code>simple</code>.</span> }

  {!props.request && <p>Only present if <InlineField>theme.type</InlineField> is <code>simple</code>.</p> }
</APIField>

<APIField name={(props.search ? "themes[x]." : "theme.") + "variables.primaryButtonTextFocusColor"} type="String" since="1.51.0" optional>
  The color of the text in the primary button when clicked. {props.request && <span><a href="https://developer.mozilla.org/en-US/docs/Web/CSS/color">Colors</a> must be a valid hex color code, RGB, or HSL value. Required if <InlineField>theme.type</InlineField> is <code>simple</code>.</span> }

  {!props.request && <p>Only present if <InlineField>theme.type</InlineField> is <code>simple</code>.</p> }
</APIField>
