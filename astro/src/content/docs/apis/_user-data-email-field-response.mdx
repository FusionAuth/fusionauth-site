import InlineField from 'src/components/InlineField.astro';
import APIField from 'src/components/api/APIField.astro';

<APIField name={props.base_field_name + '.data.email'} type="String">
  This field will be used as the email address if no <InlineField>{props.base_field_name}.email</InlineField> field is found.

  This feature was removed in version 1.26.0 and added back in 1.27.2.
</APIField>
