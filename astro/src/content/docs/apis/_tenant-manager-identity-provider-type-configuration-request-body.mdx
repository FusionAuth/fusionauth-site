import APIBlock from 'src/components/api/APIBlock.astro';
import APIField from 'src/components/api/APIField.astro';
import JSON from 'src/components/JSON.astro';

#### Request Body

<APIBlock>
  <APIField name="typeConfiguration" type="Object" required>
    The Tenant Manager identity provider type configuration object.
  </APIField>

  <APIField name="typeConfiguration.defaultAttributeMappings" type="Map<String, String>" optional>
    A map of default attribute mappings. Mapping keys not present in the configured Tenant Manager attribute form are removed before persistence.
  </APIField>
  <APIField name="typeConfiguration.defaultAttributeMappings[key]" type="String" optional>
    The source expression used to populate the mapped FusionAuth user or registration field.
  </APIField>

  <APIField name="typeConfiguration.enabled" type="Boolean" optional defaults="true">
    Whether this identity provider type is enabled for Tenant Manager.
  </APIField>

  <APIField name="typeConfiguration.linkingStrategy" type="String" optional>
    The linking strategy for this identity provider type. Required when creating or replacing a type configuration. Valid values are `LinkByEmail`, `LinkByEmailForExistingUser`, `LinkByUsername`, and `LinkByUsernameForExistingUser`.
  </APIField>

  <APIField name="typeConfiguration.type" type="String" optional>
    The identity provider type key. This value is read from the URI path parameter and any value provided in the request body is ignored.
  </APIField>
</APIBlock>

<JSON title="Example Request JSON" src="tenant-manager/identity-provider/request.json" />
