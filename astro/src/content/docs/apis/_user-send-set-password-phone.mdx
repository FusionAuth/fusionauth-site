import APIField from 'src/components/api/APIField.astro';
import InlineField from 'src/components/InlineField.astro';

  <APIField name="sendSetPasswordPhone" type="Boolean" optional defaults="false" since="1.99.9">
    If `true`, FusionAuth will send the User an SMS message asking them to set their password. The Message Template that is used is the one specified in the <InlineField>setPasswordTemplateId</InlineField> {props.set_password_field_config_blurb}.

    When you set this value to `true`, any provided <InlineField>password</InlineField> field is ignored.

    If a Messenger is not selected in the <InlineField>tenant.phoneConfiguration.messengerId</InlineField> field, this value is ignored.
  </APIField>
