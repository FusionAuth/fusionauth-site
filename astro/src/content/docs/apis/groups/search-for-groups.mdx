---
title: Search for Groups
htmlTitle: Search for Groups API
description: API documentation for the FusionAuth Search for Groups API.
order: 500
---
import API from 'src/components/api/API.astro';
import GroupSearchRequestParameters from 'src/content/docs/apis/groups/_group-search-request-parameters.mdx';
import GroupsResponseBody from 'src/content/docs/apis/groups/_groups-response-body.mdx';
import JSON from 'src/components/JSON.astro';
import StandardGetResponseCodes from 'src/content/docs/apis/_shared/_standard-get-response-codes.astro';
import XFusionauthTenantIdHeaderScopedOperation from 'src/content/docs/apis/_shared/_x-fusionauth-tenant-id-header-scoped-operation.mdx';

<AvailableSince since="1.45.0"/>

This API is used to search for Groups and may be called using the `GET` or `POST` HTTP methods. Examples of each are provided below. The `POST` method is provided to allow for a richer request object without worrying about exceeding the maximum length of a URL. Calling this API with either the `GET` or `POST` HTTP method will provide the same search results given the same query parameters.

## Request

<API method="GET" uri="/api/group/search?name={name}&tenantId={tenantId}" authentication={["api-key"]} title="Search for Groups"/>

Use a `POST` request to instead send the search criteria in a JSON request body.

<API method="POST" uri="/api/group/search" authentication={["api-key"]} title="Search for Groups"/>

#### Request Headers

<XFusionauthTenantIdHeaderScopedOperation />

### Request Parameters

<GroupSearchRequestParameters parameter_prefix="" />

### Request Body

<GroupSearchRequestParameters parameter_prefix="search." />

<JSON title="Example JSON Request" src="groups/search-post-request.json" />

## Response

The response for this API contains the Groups matching the search criteria in paginated format.

<StandardGetResponseCodes never_missing never_search_error />

### Response Body

#### Response Body

<GroupsResponseBody base_field_name="groups[x]" include_total />
