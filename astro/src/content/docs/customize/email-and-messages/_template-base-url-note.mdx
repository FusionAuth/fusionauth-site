import Aside from 'src/components/Aside.astro';

<Aside type="note">
For production, configure either the Application or Tenant <InlineField>baseURL</InlineField> to a public URL such as `https://auth.example.com` so links rendered from templates point to the correct host.
</Aside>
