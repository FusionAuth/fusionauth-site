---
title: "Quickstart: React Single-Page App (SPA)"
description: "Use Fusionauth to add authentication to your React application. Add registration, login, and a protected route to a frontend-only project. Estimated time to complete: 15 minutes"
navcategory: getting-started
prerequisites: Docker, Node 22
section: spa
technology: React.js
language: JavaScript
icon: /img/icons/react.svg
faIcon: fa-react
color: red
codeRepository: https://github.com/ritza-co/fusionauth-quickstart-react/draft_overhaulReactQuickstart
codeRoot: https://raw.githubusercontent.com/ritza-co/fusionauth-quickstart-react/draft_overhaulReactQuickstart
cta: EmailListCTA
---

import Aside from '/src/components/Aside.astro';
import NextSteps from '/src/components/quickstarts/NextSteps.astro';
import DownloadWidget from 'src/components/download/DownloadWidget.astro';
import HostedBackendWarning from 'src/content/docs/_shared/_hosted-backend-warning.md';
import InlineUIElement from 'src/components/InlineUIElement.astro';
import {RemoteCode} from '@fusionauth/astro-components';

## Overview

Add login, registration, multifactor authentication (MFA), single sign-on (SSO), and much more to your apps in days, not months, using FusionAuth.

In this quickstart you:
- Create a basic app.
- Add a public homepage and a protected account page.
- Add registration, login, and logout.
- Add a button to the account page to refresh the authentication cookie.

## Before you start

To use this quickstart you need:

- [Docker](https://www.docker.com) version 23 or later to run the container with FusionAuth.
- [Node.js](https://nodejs.org/en) version 22 or later to run the React app.
- A project directory to work in.

The <a href={frontmatter.codeRepository}>repository for this quickstart</a> includes the code for the different stages in four directories:

- **`01_start`:** Empty files.
- **`02_noAuthentication`:** The example app with placeholder pages and no authentication.
- **`03_withAuthentication`:** Adds authentication that uses FusionAuth.
- **`04_callTheServer`:** Adds refreshing the authentication cookie to the account page.

You can start from any of these points, or jump ahead to one of them if you get stuck.

## Create a React application without authentication

The initial React app has three components:

- **`index.tsx`:** The app container, which also provides routing from one page to another.
- **`home.tsx`:** The login page.
- **`account.tsx`:** The account page, which shows the user’s details.

**Step 1:** In your project directory, create `index.html` with the following content:

<RemoteCode url="https://raw.githubusercontent.com/ritza-co/fusionauth-quickstart-react/draft_overhaulReactQuickstart/02_noAuthentication/index.html" lang="tsx"/>

At `1`, this file loads the React app from the `src="/index.tsx"` landing component.

**Step 2:** Create `index.tsx` with the following content:

<RemoteCode url="https://raw.githubusercontent.com/ritza-co/fusionauth-quickstart-react/draft_overhaulReactQuickstart/02_noAuthentication/index.tsx" lang="tsx"/>

This landing component creates an `App` component that routes `/account` to the account component, and routes `/` and all other URLs to the home component. The code then uses React to insert the app into the page as the `root` element.

**Step 3:** Create the public homepage, `home.tsx`, with the following content:

<RemoteCode url="https://raw.githubusercontent.com/ritza-co/fusionauth-quickstart-react/draft_overhaulReactQuickstart/02_noAuthentication/home.tsx" lang="tsx"/>

**Step 4:** Create the protected account page, `account.tsx`, with the following content:

<RemoteCode url="https://raw.githubusercontent.com/ritza-co/fusionauth-quickstart-react/draft_overhaulReactQuickstart/02_noAuthentication/account.tsx" lang="tsx"/>

The homepage and the account page are placeholders for future content, with buttons that redirect the user from one page to the other with no authentication.

**Step 5:** Create the stylesheet, `index.css`, with the following content:

<RemoteCode url="https://raw.githubusercontent.com/ritza-co/fusionauth-quickstart-react/draft_overhaulReactQuickstart/02_noAuthentication/index.css" lang="css"/>

### Run the base app

**Step 6:** Open a terminal and change your current directory to the project directory.

**Step 7:** Run the commands below to install the Node.js packages for React:

```sh
npm install  @fusionauth/react-sdk@~2.5.1  react@~19.1.1  react-dom@~19.1.1  react-router-dom@6.7.0

npm install --save-dev  @eslint/js@~9.33.0  @types/react@~19.1.10  eslint@~9.33.0  typescript@~5.9.2  typescript-eslint@~8.40.0  parcel@~2.15.4
```

**Step 8:** Run the command below to start the web server:

```sh
npx parcel index.html --host 0.0.0.0 --port 3000
```

**Step 9:** Open a browser and view http://localhost:3000 to see the site.

![Login page with no authentication](../../../public/img/quickstarts/quickstart-javascript-react-web/login.webp)

You’re able to use the login button to open the account page because there’s no authentication or protected URLs.

## Download and run FusionAuth

Next, set up FusionAuth as the authentication provider by installing and running a preconfigured version of FusionAuth in a Docker container.

Download and run a Docker container with FusionAuth:

<DownloadWidget kickstartEnabled={true} kickstartName={"quickstart-spa"} />

<Aside type="note">
In this example, FusionAuth has already been configured to communicate with an app on http://localhost:3000, where the example React app is running. [Kickstart](/docs/get-started/download-and-install/development/kickstart) does this configuration and sets up example user accounts.

Your React app needs only a static web server because FusionAuth provides a [hosted backend](/docs/apis/hosted-backend) for purely frontend apps.
</Aside>

## Add authentication to the app with FusionAuth

In this section, you update the `.tsx` files to use FusionAuth authentication.

**Step 1:** Add the following code to the top of `index.tsx`:

<RemoteCode url="https://raw.githubusercontent.com/ritza-co/fusionauth-quickstart-react/draft_overhaulReactQuickstart/03_withAuthentication/index.tsx" lang="tsx" tags="a"/>

The line above adds FusionAuth capabilities to the app with the [React SDK](/docs/sdks/react-sdk).

**Step 2:** Add the following code to `index.tsx` after the `import` statements:

<RemoteCode url="https://raw.githubusercontent.com/ritza-co/fusionauth-quickstart-react/draft_overhaulReactQuickstart/03_withAuthentication/index.tsx" lang="tsx" tags="b"/>

The `FusionAuthProviderConfig` configures the `@fusionauth/react-sdk` to talk to FusionAuth on the correct domain and port. Normally, authentication configuration happens on the server, and you need to hide secrets from the public. But you don't need to protect `clientId` since this app uses the FusionAuth hosted backend.

**Step 3:** Add the following code in the top of the `App()` function:

<RemoteCode url="https://raw.githubusercontent.com/ritza-co/fusionauth-quickstart-react/draft_overhaulReactQuickstart/03_withAuthentication/index.tsx" lang="tsx" tags="c"/>

This code prevents the page from displaying if the user is busy logging in.

**Step 4:** Overwrite the code at the bottom of `index.tsx` with the following code:

<RemoteCode url="https://raw.githubusercontent.com/ritza-co/fusionauth-quickstart-react/draft_overhaulReactQuickstart/03_withAuthentication/index.tsx" lang="tsx" tags="d"/>

The `<FusionAuthProvider {...config}>` wraps the app and adds authentication features to all the components.

To use those features, add code to redirect the user to FusionAuth for login and to check whether they are logged in.

**Step 5:** Add the following code below to the top of `home.tsx`:

<RemoteCode url="https://raw.githubusercontent.com/ritza-co/fusionauth-quickstart-react/draft_overhaulReactQuickstart/03_withAuthentication/home.tsx" lang="tsx" tags="a"/>

**Step 6:** Change the code in the top of the `Home()` function to the following code:

<RemoteCode url="https://raw.githubusercontent.com/ritza-co/fusionauth-quickstart-react/draft_overhaulReactQuickstart/03_withAuthentication/home.tsx" lang="tsx" tags="b"/>

The `useFusionAuth()` line adds helper functions to the homepage code. The user is redirected to the account page if `isLoggedIn` is true.

**Step 7:** Add the click handler below to the login button:

<RemoteCode url="https://raw.githubusercontent.com/ritza-co/fusionauth-quickstart-react/draft_overhaulReactQuickstart/03_withAuthentication/home.tsx" lang="tsx" tags="c"/>

The `startLogin()` line adds authentication to the login button.

**Step 8:** Add the following code to the top of `account.tsx`:

<RemoteCode url="https://raw.githubusercontent.com/ritza-co/fusionauth-quickstart-react/draft_overhaulReactQuickstart/03_withAuthentication/account.tsx" lang="tsx" tags="a"/>

**Step 9:** Add the following code to `account.tsx` after the `import` statements:

<RemoteCode url="https://raw.githubusercontent.com/ritza-co/fusionauth-quickstart-react/draft_overhaulReactQuickstart/03_withAuthentication/account.tsx" lang="tsx" tags="b"/>

To see when authentication expires and inform the user later in a label, the `getExpiryTimeInSeconds()` function checks the `app.at_ex` cookie.

**Step 10:** Change the code in the top of the `Account()` function to the following code:

<RemoteCode url="https://raw.githubusercontent.com/ritza-co/fusionauth-quickstart-react/draft_overhaulReactQuickstart/03_withAuthentication/account.tsx" lang="tsx" tags="c"/>

Similar to the homepage, `useFusionAuth()` adds the helper functions, and `startLogout()` adds authentication to the logout button. The `useEffect(() => {` section that uses the expiry time function updates the label on the screen every second.

**Step 11:** Add the user's email address to the titlebar with the following code:

<RemoteCode url="https://raw.githubusercontent.com/ritza-co/fusionauth-quickstart-react/draft_overhaulReactQuickstart/03_withAuthentication/account.tsx" lang="tsx" tags="d"/>

**Step 12:** Change the expiry label at the bottom of `account.tsx` to the following code:

<RemoteCode url="https://raw.githubusercontent.com/ritza-co/fusionauth-quickstart-react/draft_overhaulReactQuickstart/03_withAuthentication/account.tsx" lang="tsx" tags="e"/>

**Step 13:** Reopen the app at http://localhost:3000. Click the login button and log in with these user credentials:

- **Username:** `richard@example.com`
- **Password:** `password`

The app uses redirection to FusionAuth for login. The port number changes in the URL before FusionAuth (on port 9011) returns you to the app (on port 3000).

![Log in with FusionAuth](../../../public/img/quickstarts/quickstart-javascript-react-web/fa.webp)

![Account page](../../../public/img/quickstarts/quickstart-javascript-react-web/account.webp)

Authentication protects your private pages, such as the account page. If you change the URL in the address bar to browse to the public homepage or protected account page, you’re redirected based on whether you’re logged in or not.

## Call FusionAuth only when authenticated

Once a user has successfully logged in, FusionAuth stores an `http-only` cookie in the user's browser. In future, if you use a full web server instead of this example's static file host, you can check for the `http-only` cookie to verify that the user is authenticated.

For calls to the FusionAuth API, you need an API key, and thus for security's sake, you need to make the calls from a server instead of the browser. Since this example doesn't use a server, almost no calls to the FusionAuth API can be made.

To illustrate calling FusionAuth from the browser only when you're logged in, in this section you add a call to [refresh the user's authentication cookie](/docs/apis/hosted-backend#refresh). (Note that this call isn't necessary because you set `shouldAutoRefresh: true` in the `FusionAuthProviderConfig` in `index.tsx`, which refreshes the user's cookie when it expires anyway.)

**Step 1:** Insert the following code into `account.tsx` at the bottom of the file, just before the closing `</div>` tag for the element `<div className="userInfoGrid">`, and save.

<RemoteCode url="https://raw.githubusercontent.com/ritza-co/fusionauth-quickstart-react/draft_overhaulReactQuickstart/04_callTheServer/account.tsx" lang="tsx" tags="fusionauth"/>

**Step 2:** Browse to the account page and click the refresh button. Notice your authentication expiration resets.

![Account page with refresh button](../../../public/img/quickstarts/quickstart-javascript-react-web/refreshButton.webp)

## Next steps

This is a quick introduction to using FusionAuth. To learn more about using FusionAuth with your product, see the [Get Started](/docs/get-started).

FusionAuth has many features, most of which are free. Some require other versions. For details, see the [pricing page](https://fusionauth.io/pricing).

For more general questions, please ask in [the FusionAuth forum](https://fusionauth.io/community/forum).
