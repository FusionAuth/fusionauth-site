---
title: Date and time utilities
description: Here is a collection of browser-based date and time utilities that can help you with conversions and calculations.
author: Brian Pontarelli
faIcon: fa-lock
color: green
disableTOC: true
---
import { border } from "../../components/BaseInput.js";
import Text from "../../components/Text.astro";

## Current date/time

These values are based on the current instant. The milliseconds and seconds are from Epoch UTC (January 1, 1970). You can copy these values but they aren't editable.

<div className="gap-6 grid grid-cols-[1fr_200px] items-center mt-8 no-prose">
  <div class:list={[border(false, false, true), "flex items-center justify-between py-1 px-2"]}>
    <span id="current-time-milliseconds">Milliseconds</span>
    <span data-widget="copy-button" data-copy-source="current-time-milliseconds"><i className="fa-light fa-copy"></i></span>
  </div>
  <span>Milliseconds</span>
  <div class:list={[border(false, false, true), "flex items-center justify-between py-1 px-2"]}>
    <span id="current-time-seconds">Seconds</span>
    <span data-widget="copy-button" data-copy-source="current-time-seconds"><i className="fa-light fa-copy"></i></span>
  </div>
  <span>Seconds</span>
  <div class:list={[border(false, false, true), "flex items-center justify-between py-1 px-2"]}>
    <span id="current-date">Date</span>
    <span data-widget="copy-button" data-copy-source="current-date"><i className="fa-light fa-copy"></i></span>
  </div>
  <span>Date</span>
  <div class:list={[border(false, false, true), "flex items-center justify-between py-1 px-2"]}>
    <span id="current-time">Time</span>
    <span data-widget="copy-button" data-copy-source="current-time"><i className="fa-light fa-copy"></i></span>
  </div>
  <span>Time</span>
</div>

## Convert from seconds

These values are based on an instant, which is either milliseconds and seconds are from Epoch UTC (January 1, 1970). You can modify the top value to change the output below it.

<Text id="seconds" name="seconds" label="Seconds or milliseconds"/>

<div className="gap-6 grid grid-cols-[1fr_200px] items-center mt-8 no-prose">
  <div class:list={[border(false, false, true), "flex items-center justify-between py-1 px-2"]}>
    <span id="date-when-seconds">Date (when seconds)</span>
    <span data-widget="copy-button" data-copy-source="date-when-seconds"><i className="fa-light fa-copy"></i></span>
  </div>
  <span>Date (when seconds)</span>
  <div class:list={[border(false, false, true), "flex items-center justify-between py-1 px-2"]}>
    <span id="date-when-milliseconds">Date (when milliseconds)</span>
    <span data-widget="copy-button" data-copy-source="date-when-milliseconds"><i className="fa-light fa-copy"></i></span>
  </div>
  <span>Date (when milliseconds)</span>
</div>

## Convert from strings

These values are based on a date in ISO format. You can modify the top value to change the output below it.

<Text id="date" name="date" label="Date/time string"/>

<div className="gap-6 grid grid-cols-[1fr_200px] items-center mt-8 no-prose">
  <div class:list={[border(false, false, true), "flex items-center justify-between py-1 px-2"]}>
    <span id="seconds-since-epoch">Seconds</span>
    <span data-widget="copy-button" data-copy-source="seconds-since-epoch"><i className="fa-light fa-copy"></i></span>
  </div>
  <span>Seconds</span>
  <div class:list={[border(false, false, true), "flex items-center justify-between py-1 px-2"]}>
    <span id="milliseconds-since-epoch">Milliseconds</span>
    <span data-widget="copy-button" data-copy-source="milliseconds-since-epoch"><i className="fa-light fa-copy"></i></span>
  </div>
  <span>Milliseconds</span>
</div>

<script type="text/javascript" src="/js/DateTime-0.1.0.js" defer></script>