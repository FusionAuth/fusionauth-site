---
import Diagram from "../../../components/mermaid/FlowchartDiagram.astro";
const { alt } = Astro.props;

//language=Mermaid
const diagram = `
flowchart TD
  cbweb["Change Bank Web App"] -->|Login| cbidp[("Change Bank IdP")]
  cbapi["Change Bank API"] -->|Login| cbidp
  bbweb["Budget Buddy Web App"] -->|Login| bbidp[("Budget Buddy IdP")] 
  bbgb["Budget Buddy Get Balance"] -->|Login - Consent| cbidp

`;
---
<Diagram code={diagram} alt={alt}/>

