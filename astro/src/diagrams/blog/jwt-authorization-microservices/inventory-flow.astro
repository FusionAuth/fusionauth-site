---
import Diagram from "../../../components/mermaid/SequenceDiagram.astro";
const { alt } = Astro.props;

//language=Mermaid
const diagram = `
sequenceDiagram

participant api as API client
participant Gateway
participant Products
api ->> Gateway : Request branches/1/products
Gateway ->> Gateway : Generate JWT
Gateway ->> Products : Request branches/1/products + JWT
Products ->> Products : Verify JWT via middleware
Products -->> Gateway : branches/1/products data
Gateway -->> api : branches/1/products data
`;
---
<Diagram code={diagram} alt={alt}/>

