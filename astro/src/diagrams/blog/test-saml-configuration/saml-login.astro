---
import Diagram from "../../../components/mermaid/SequenceDiagram.astro";
const { alt } = Astro.props;

//language=Mermaid
const diagram = `
sequenceDiagram

participant User
participant SP as IAMShowcase (SP)
participant IdP as FusionAuth (IdP)

User->>SP: Requests access to resource
SP->>SP: Generate SAML AuthnRequest
SP->>User: Redirect user to IdP with SAML AuthnRequest
User->>IdP: Send SAML AuthnRequest to FusionAuth
IdP->>IdP: Authenticate user (e.g., login)
IdP->>User: Respond with SAML Assertion
User->>SP: Send SAML Assertion to IAMShowcase
SP->>SP: Validate SAML Assertion
SP->>User: Grant access to resouce
`;
---
<Diagram code={diagram} alt={alt}/>