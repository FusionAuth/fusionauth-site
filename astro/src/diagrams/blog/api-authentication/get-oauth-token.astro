---
import Diagram from "../../../components/mermaid/SequenceDiagram.astro";
const { alt } = Astro.props;

//language=Mermaid
const diagram = `
sequenceDiagram

actor User as User/Browser
participant App as App
participant AS as Authorization Server
User ->> App : View Initial Page, Click Login
App ->> User : Redirect User To FusionAuth With Scopes
User ->> AS : Request Login Page
AS ->> AS : Return Login Page
User ->> AS : Provides Credentials
AS ->> User : Redirect With Authorization Code
User ->> App : Request Redirect URI
App ->> AS : Request Tokens
AS ->> App : Return Tokens
App ->> User : Return Tokens
User ->> User : Store Tokens
`;
---
<Diagram code={diagram} alt={alt}/>
