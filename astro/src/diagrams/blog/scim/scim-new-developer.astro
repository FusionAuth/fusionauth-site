---
import Diagram from "../../../components/mermaid/SequenceDiagram.astro";
const { alt } = Astro.props;

//language=Mermaid
const diagram = `
sequenceDiagram

participant hradmin as HR Admin
participant hris as HR System
participant cloud as Cloud Computing Provider
participant scs as Source Control System
participant ppts as Ping Pong Tournament System

hradmin ->> hris : Add user
hris ->> cloud : Provision User
hris ->> scs : Provision User
hris ->> ppts : Provision User
`;
---
<Diagram code={diagram} alt={alt}/>

