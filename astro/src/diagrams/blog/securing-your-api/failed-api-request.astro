---
import Diagram from "../../../components/mermaid/SequenceDiagram.astro";
const { alt } = Astro.props;

//language=Mermaid
const diagram = `
sequenceDiagram

actor User as Dinesh
participant API as API Server
User ->> API : Get Richard's todos
API ->> API : Request validation attempted, fails
API ->> User : Error message
`;
---
<Diagram code={diagram} alt={alt}/>
