---
import Diagram from "../../../components/mermaid/SequenceDiagram.astro";
const { alt } = Astro.props;

//language=Mermaid
const diagram = `
sequenceDiagram

actor User as Richard
participant RS as Todos API Server
participant AS as Central Authority
RS ->> AS : Retrieve Public Keys 
Note right of User : ... Time passes ...
User ->> RS : Present API Key, Request Data
RS ->> RS : Check Signature
RS ->> RS : Check Data in Key
RS ->> User : Return Todos Data
`;
---
<Diagram code={diagram} alt={alt}/>
