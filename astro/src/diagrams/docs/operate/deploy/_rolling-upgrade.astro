---
import Diagram from 'src/components/mermaid/FlowchartDiagram.astro';

//language=Mermaid
const code = `
graph TB
    A(Users) --> B(Load Balancer)
    B --> |Switch traffic when ready <br/> Upgrade one node at a time| C(Node 1)
    B --> D(Node 2)
    B --> E(Node 3)
    style C fill:#C0F0C0
`;
---
<Diagram {code} />

