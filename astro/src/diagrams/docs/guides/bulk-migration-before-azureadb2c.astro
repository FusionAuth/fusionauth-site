---
import Diagram from "../../../components/mermaid/SequenceDiagram.astro";
const { alt } = Astro.props;

//language=Mermaid
const diagram = `
sequenceDiagram

actor user as User Agent
participant ad as Azure AD B2C

user ->> ad  : Request login page
ad -->> user  : Receive login page
user ->> ad  : Post credentials
ad ->> ad : Verify credentials
ad -->> user  : Display results
`;
---
<Diagram code={diagram} alt={alt}/>

