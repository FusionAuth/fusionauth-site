---
import Layout from 'src/layouts/Docs.astro';
import { getDocNavContext, getStaticDocPaths } from 'src/tools/docs';

export const getStaticPaths = getStaticDocPaths('lifecycle/manage-users/account-management/');

const navContext = await getDocNavContext(Astro);
const { entry } = Astro.props;
const { Content, headings } = await entry.render();
---
<Layout frontmatter={entry.data} {headings} {navContext}>
  <Content />
</Layout>
