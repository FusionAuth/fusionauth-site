==== Response Body

[api]
ifeval::["{base_field_name}" != "ipAccessControlList"]
[field]#ipAccessControlLists# [type]#[Array]#::
The list of all IP ACLs
endif::[]

[field]#{base_field_name}.entries# [type]#[Array]#::
A list of IP ranges and the action to apply for each.  One and only one entry will have a `startIPAddress` of `*` to indicate the default action of the IP ACL.

[field]#{base_field_name}.entries``[y]``.action# [type]#[String]#::
The action to take for this IP Range.

[field]#{base_field_name}.entries``[y]``.endIPAddress# [type]#[String]#::
The ending IP (IPv4) for this range.

[field]#{base_field_name}.entries``[y]``.startIPAddress# [type]#[String]#::
The starting IP (IPv4) for this range.

[field]#{base_field_name}.name# [type]#[String]#::
The name of this IP ACL.

[source,json]
.Example IP ACL response JSON

----
include::../../../src/json/ip-acl/search-response.json[]
----
