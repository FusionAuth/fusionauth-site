-# Modified for FusionAuth
-#    I changed this to use a <section> instead of a <div>
-#    I removed the nested <div class="sectionbody">
%section.preamble
  =content
  - if (attr? :toc) && (attr? 'toc-placement', 'preamble')
    #toc{:class=>(attr 'toc-class', 'toc')}
      #toctitle=(attr 'toc-title')
      =converter.convert @document, 'outline'
