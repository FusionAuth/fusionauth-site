<ul>
  {% assign apps = site.data.exampleapps | where: "language", language %}
  {% for app in apps %}
    <li>
      <a href="{{ app.url }}">
        {{ app.name }}
      </a> - {{app.description}} 
    </li>
  {% endfor %}
</ul>

