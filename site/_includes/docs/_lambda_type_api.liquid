{% if requestParameter != "true" %}
  {% assign fieldPrefix = 'lambda.' %}
{% else %}
  {% assign fieldPrefix = '' %}
{%endif %}

[field]#{{fieldPrefix}}type# [type]#[String]# [required]#Required#::
The lambda type.  The possible values are:
+
{% assign lambdas = site.data.lambdas | sort: 'typeText' %}
{% for lambda in lambdas %}
* `{{lambda.typeText}}` {%if lambda.version%}[since]#Available since {{lambda.version}}#{%endif%}
{% endfor %}
