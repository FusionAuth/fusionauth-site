{% assign lambdas = site.data.lambdas | sort: 'fieldName' %}
{% for lambda in lambdas %}
{% assign anchorTag = template.displayName | replace: ' ', '-' | downcase %}
* link:{{lambda.docLink}}[{{lambda.displayName}}]
{% endfor %}
